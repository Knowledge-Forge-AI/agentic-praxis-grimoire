#!/usr/bin/env python3
"""Append one validated operational report record."""

from __future__ import annotations

from pathlib import Path
import sys


_root = Path(__file__).resolve().parent.parent
sys.path.insert(0, str(_root / "src"))

from agentic_praxis_grimoire.cli import legacy_main  # noqa: E402


if __name__ == "__main__":
    raise SystemExit(legacy_main("append-operational-report", sys.argv[1:]))
