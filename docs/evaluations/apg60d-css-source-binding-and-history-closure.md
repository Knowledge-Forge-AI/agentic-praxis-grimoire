# APG60D CSS Source-Binding and Phase-History Closure

APG60D is the final pre-authoring correction after accepted APG60C. It keeps
the frozen sixty-case CSS behavior contract byte-identical, authors no
candidate, creates no ADR, and changes no public, active, release, maturity,
route, projection, catalog, or target surface.

## Disposition

APG60C remains accepted. APG60D corrects two newly reproduced foundation
defects forward:

- the prior static checker accepted arbitrary callback and protocol execution
  while the surrounding record claimed stronger runtime and call closure; and
- future lifecycle gates did not require the complete APG60C, APG61, and APG62
  phase-history chain.

The corrected Python contract proves one exact immutable static declaration
and refuses syntactically bound or mechanically identified protected-name
writes. It does not use runtime value as closure authority and does not
represent arbitrary caller, callback, protocol, general library-mediated
reflective call effects, interpreter, or host mutation as prevented. Owner
modules are not imported to extract the audited declarations.

## Exact phase history

The versioned phase-history manifest freezes exact bundles for APG58 through
APG62. Every selected bundle requires its public evaluation, exact exit,
publication-excluded index, every declared private record, and its applicable
exact decision-ADR path/status role as direct regular UTF-8 files beneath
direct directory ancestors. Unknown, duplicate, missing, empty,
wildcard-partial, dangling, external-symlink, and in-repository symlink
substitutions fail. The manifest itself has the same no-follow ancestor
boundary. Managed reports and Git objects remain distinct delivery evidence
and are not misrepresented as repository files.

Pre-authoring and authored states require the complete APG58 through APG60D
foundation. Authored Proposed state additionally requires exact APG61 history.
Retained and rejected states require exact APG61 authoring plus APG62 terminal
history. Existing decision and current-surface gates continue to distinguish
retention from rejection.

APG60D owns exit `00084`; future APG61 and APG62 exits are `00085` and
`00086`.

## Preserved contract and boundary

The CSS contract remains sixty cases, `APG60-CSS-001` through
`APG60-CSS-060`, with unchanged expected objects and unchanged
`300/600/900` policy. CSS remains rejected-evidence pre-authoring policy, not
active guidance. Development remains 28 skills, 28 catalog rows, and 28
projections with fourteen stable and fourteen provisional skills. ADR 0031,
ADR 0034, and ADR 0035 remain Rejected; ADR 0036 remains unused.

No target command or mutation, CSS authoring or integration, other Web or Node
work, readiness, publication, deployment, APG61, or successor begins.

## Verification

Focused failing-first controls reproduced the callback/protocol acceptance,
missing-foundation, incomplete-terminal-history, wildcard, symlink, and exit
identity defects before correction. Corrected source-binding, phase-bundle,
all-five-state lifecycle, current-survivor, traceability, narrative, decision,
and anti-regression checks pass. Full repository and delivery evidence is
recorded in the associated managed APG60D report.

The superseding publication-excluded APG61 handoff recommends a separately
authorized fresh authoring phase. This evaluation does not grant that
authority.

## Subsequent correction

APG60E preserves this accepted result and corrects the distinct
repository-path provenance gap forward. The APG60D object, report, and
historical phase-history manifest remain unchanged.
