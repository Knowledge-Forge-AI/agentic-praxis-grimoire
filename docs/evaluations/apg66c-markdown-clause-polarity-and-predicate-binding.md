# APG66C Markdown Clause-Polarity and Predicate Binding

Date: 2026-08-01

## Result

Complete — accepted Markdown semantics remain unchanged while coordinated
numeric predicates, rollback obligations, source and signal polarity, targeted
contradictions, and the repository-import cache-restoration test boundary are
closed with order-independent evidence.

## Accepted baseline

APG66B remains accepted as delivered at commit
`db847b287321a087c7413b16c989c15e07a3b167`, tree
`3c0e559d46e8145da314087a23a112c4eacc4fe0`, with sole parent exact APG66A
`9cfd8be90cc39cf87caea4d75e7eb4ab432aef40`. Its exact Git-show and associated
operational records remain
`GIT-SHOW-REPORT-db847b287321a087c7413b16c989c15e07a3b167` and
`OPERATIONAL-REPORT-ae82421289b68923b943680f5acfa34d3ee7c5adedc6b3a069f1753777a979a2`.
Fresh parsing found exactly those two complete records in order; local and
fetched main and APG66B refs were equal before implementation.

## Failing-first result

Exact APG66B reproduced all five audit families before correction: eight
coordinated-count cases, seven subject-first or contradicted rollback cases,
all 102 generic source-row contradiction controls plus six reported variants,
55 signal-negation controls, and 36 targeted-guard contradictions failed. The
completed APG66B exit also reproduced its mechanically incorrect `Intended
terminal state` heading, corrected forward by APG66C.

## Selected bounded model

The numeric guard retains a count subject across named coordinators, punctuation,
and a following predicate until a new explicit subject appears. A forbidden
inherited predicate controls over an earlier or later negative. Descriptive
inspection remains allowed; no preferred count or general prose parser is
introduced.

Clause guards return `positive`, `negative`, `conflicted`, `absent`, or
`human-review-required`. Passing machine evidence requires a local positive
without a direct contradiction. Explicitly negated tokens are not observations.
Rollback is evaluated only in its mapped scenario scope. Source, signal, and
targeted guards use exact closed tokens plus bounded attached polarity; quoted,
prohibited, global-only, and larger-token forms do not pass.

The retained machine surface has 17 source rows, 11 signal instances across
7 rows, and 18 targeted guards. No accepted field is newly downgraded: the two
rollback distinctions already assigned to human review remain there. Arbitrary
paraphrase, implicit contradiction, complex discourse, and complete 34-row
natural-language sufficiency remain APG66 human authority.

## Proof boundary and preserved state

Layer A remains accepted architecture, lean-contract, register/carrier identity,
36-row shape, 34/2 split, and closed schema. Layer B remains exact ordered
vocabulary, exact register-to-fixture projection, process-row exclusion, and
fixture-disagreement refusal. Layer C owns candidate structure, clause and
coverage navigation, normative-spec containment, exact tokens, and only the
mutation-controlled polarity claims above.

The Markdown leaf, specification, coverage, register, fixture, and ADR semantics
are unchanged. ADR 0037 and ADR 0038 remain Accepted with amendment.
`markdown-language-profile` remains retained provisional at 29/29/29, 14 stable
/ 15 provisional, and 27 general / 1 ChatGPT-local / 28 checked routes. CSS
remains absent and rejected. Corrected public and active v0.4.0 and both
read-only targets remain unchanged.

## Cache-restoration continuation

The first invocation correctly stopped before commit and preserved exact
`GIT-DIFF-REPORT-360e91ce42620af49e29699443780f548aef22542f4aea28769ebb2f7344566d`
and associated
`OPERATIONAL-REPORT-0800e81597909e009b5ba4eb97b06c75dbc0158b000ebf9b1480f7f0ce6d928e`.
The continuation reproduced the isolated failure and classified it as an
overbroad test assertion: the test required equality of the entire process
module cache although the accepted contract covers inspected-root modules,
colliding local names, parent search paths, and inspected-root importer entries.
No inspected-root module survived, no pre-existing relevant object changed, and
the parent search path was exact.

A test-only relevant-state helper now distinguishes unrelated first-loaded host
or standard-library dependencies from repository-consumer residue. Adverse
controls still reject inspected-root modules, relevant object replacement,
search-path and importer-cache residue, cross-root reuse, standard-library
shadowing, and exceptional-path leakage. The import owner passes alone, in
both explicit preload orders, twice in one process, around the complete Markdown
selection, and through configured xdist. Production repository-import bytes are
unchanged; APG60F/APG60G isolation remains in force.

## Verification and boundary

The original blocked snapshot's focused 1,289/1,294 results remain historical.
Final complete Markdown owners pass 1,386 unit tests and 1,391 with integration;
the C1 owners pass 45 serial and xdist tests, and the combined focused unit lane
passes 1,431. Final regression passes 2,614 unit tests; 563 integration tests
with two expected skips; 6,822/7,293 combined-union statements and 2,416/2,686
branches; and 23/23 configured Bats tests.

The clean current-development candidate passes the complete public-release
checker. Historical corrected-v0.4 reconstruction remains exact at tree
`75639775ec7174135a4c6863721cb684f026bd28`, commit
`d23a09477a2da8e4ea2214910a5a0de6e9be24b5`, and annotated tag
`96c2647d177c0d03500a8cf7380c56a56d69188e`; it excludes the new current-only
cache helper, test, Markdown helper, test, and phase records. Independent
behavior, structure, integration, complete-diff, privacy, rights, and concrete-
message review accepts the frozen correction.

APG67 is recommended at exit 00099 but not begun. No readiness, publication,
deployment, APG67, or successor is authorized.

## APG66D forward correction

APG66C remains accepted as delivered, including its C1 classification and
bounded relevant-state design. APG66D later corrected one narrower assertion
gap: a pre-existing relevant `sys.modules` key containing `None` could be
deleted because the helper used `dict.get()` for identity comparison. APG66D
requires explicit key presence and then exact stored-object identity. It
changes no production importer byte, Markdown semantic, or ADR decision.
APG66D consumes exit 00099, so APG67 is now recommended at exit 00100 but
remains unbegun and separately authorized.
