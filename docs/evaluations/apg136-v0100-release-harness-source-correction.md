# APG136 v0.10.0 release harness and source correction

Status: terminal closeout qualified; dispatcher work-review findings amended.
Development Git finalization remains dispatcher-owned. Phase APG136 uses exit 00181 without substitution.

Proposal disposition: **amend**. The original assignment remains source and
release-harness correction. All seven planning findings are incorporated:
version-core policy binding, unchanged projected schemas, exclusion-file
membership, maintained runner-test ownership, parsed workflow assignments,
bounded historical policy regression, and explicit pytest initial paths.
No stage delta was reported. Internal worker findings are implementation
evidence and do not satisfy a dispatcher checkpoint.

## Historical inventory and disposition

APG135's blocked outcome is preserved. Its deterministic candidate A/B
construction succeeded, but its checks were incomplete. Those candidate
identities are invalid for release after source correction. APG135 did not
produce a qualified distribution bundle.

APG136 completed both historical checker commands naturally. Both reported
the same 24 failed tests, 4,371 passes, 24 skips, seven setup errors and 1,504
passing subtests. All preceding checker categories passed. Because the checker
stops at a failed category, the otherwise-unreached confidentiality category
was separately run against unchanged diagnostic copies and passed for both.
These supplemental observations are not presented as full checker successes.

The complete inventory confirmed the three known classes: stale workflow
fixtures, missing inherited public deselections and hidden-directory pruning.
It also exposed a disposable Git fixture lacking a local synthetic author and
colliding nested pytest probe module names. Both additional defects were
independently reproduced under the public checker's isolation conditions and
corrected in their owning tests.

The initial diagnostic environment incorrectly relocated retained package
inputs and supplied an owned root with incorrect permissions. After correcting
only that diagnostic setup, all affected files were rerun against both unchanged
candidates: each reported 55 passes and the same one genuine Git fixture
failure. No package was installed or upgraded. This accounts for setup-related
failures without introducing test exclusions.

## Resulting contracts

The workflow fixtures derive tags and distribution names from canonical
`VERSION`. Unit checks parse the verifier's assignments; integration negatives
exercise mismatched fixture versions and tags. Trusted Publishing production
semantics are unchanged.

The canonical runner passes the exact validated inventory suite as explicit
pytest initial files. The same selection governs each worker's collection:
every file contributes a node, no foreign file is admitted, node IDs are unique,
and all workers report the same complete ordered collection exactly once.
Ambiguous path aliases and duplicate selected paths fail. Multiple distinct
nodes in one file remain valid. Existing terminal-result, lifecycle and
coverage accounting remain enforced; no zero-test exception is introduced.
Runner unit tests own receipt negatives, and the private runner integration
oracle exercises the actual plugin with hidden and foreign files.

Public deselections are code-owned by audited release version. Versions 0.7.0,
0.8.0, 0.8.1, 0.9.0 and 0.10.0 receive exactly the existing seven reviewed
node exclusions, including the current version's seventeen additive Python
files. Earlier policies receive none. Unknown, malformed or mismatched policy
identities fail closed, and every excluded node must belong to the audited
Python selection. Projected policy keys and historical surfaces are unchanged.

The final selection audit found two projected integration files outside the
unchanged 151-file audited tuple. To satisfy complete projected-test execution,
current v0.10 execution additionally derives its complete 153-file selection
from the validated canonical inventory. The audited tuple still contains its
seventeen additive files, all seven exclusions remain exact, and earlier
versions retain their original selection without consulting current inventory.
This is an explicit amendment from observed source state, not a new deselection
or a historical policy rewrite. Both additional files pass the final source
gate and the fresh diagnostic pair.

The first corrected A/B checks also exposed four failures with one shared cause:
an existing CSS evidence test module skipped collection entirely when private
evidence was absent. The repaired collection invariant correctly refused the
missing file. The same absence predicate now marks its 68 tests as skipped,
preserving their collection and terminal accounting. Parameter bindings are
resolved only when those tests run; their node identities and development-source
assertions are preserved. No new exclusion, skip condition or allowed-zero-file
exception is introduced. A real subprocess regression proves that absence
produces collected skipped nodes, while all 68 tests still pass with evidence.

Focused qualification also corrected two stale test prerequisites: an oracle
bootstrap probe now imports a maintained helper, and synthetic release fixtures
bind their version to the policy they actually construct. These changes preserve
the intended assertions rather than altering historical release contracts.

The existing large runner and release modules are retained as bounded legacy
owners. This phase adds selection and policy checks at their existing boundaries;
an unrelated decomposition would expand the release correction unnecessarily.

## Producer verification boundary

The collection audit selects all 112 unit and 41 integration files. The only
hidden inventory paths are the two release-workflow tests. Historical recursive
discovery omitted both, losing two unit and eighteen integration nodes. The
repaired source and public audits collect both. The final canonical gate passes
3,810 unit tests and 730 integration tests with two integration skips. Each of eight workers has the
same complete collection in each suite. All three workflow unit nodes and all
twenty-one workflow integration nodes have passing terminal results.

| Coverage gate | Statements covered/total | Branches covered/total |
| --- | --- | --- |
| Unit | 10,237/11,923 | 3,720/4,626 |
| Integration | 10,244/11,923 | 3,709/4,626 |
| Combined union | 10,807/11,923 | 4,033/4,626 |

Every gate passes with unchanged thresholds, exclusions and arithmetic. The
denominator reflects the corrected source; no denominator reduction was used.
Focused runner/private-oracle qualification passes 170 tests, runner integration
passes 30 tests, and workflow/publication qualification passes 90 tests. Record
identity, skill-library, Python compile/import and whitespace checks pass.
Final gate receipts confirm 115 passing release-checker unit/integration tests.
The focused public-selection amendment passes 64 unit tests and 118 subtests.
All four isolated public runner regressions pass after the skip-visibility correction.
The two additional public files pass eleven tests in clean isolation. The public
collection audit has the same 3,810 unit and 732 integration nodes as source,
with no missing or foreign file.
The skill-library invocation uses the required absolute repository root; an
earlier relative-root invocation failure is retained as diagnostic evidence.

Historical regression means deterministic policy reconstruction, exact resolver
equality and maintained checker integration for the five specified versions.
It does not mean rebuilding historical distribution bundles or editing old
release evidence. Corrected A/B candidates are disposable diagnostics only.
Distribution output exercised by required tests remains disposable fixture
output and is not qualified as a final release bundle.

Both final corrected-source A/B checks passed to natural completion, including
confidentiality. Each reports 4,444 passes, 91 skips, exactly seven existing
deselections and 1,547 passing subtests. Their Git objects and complete manifests
match. The existing private-evidence absence condition accounts for 68 visible
skipped nodes; it no longer removes that file from collection. Earlier failed
diagnostic pairs remain recorded and are superseded by this final pair.

At the producer checkpoint, all behavior-bearing corrected files matched the
disposable source snapshot used for these checks. Terminal changes below are
separately qualified and are not represented by that historical byte binding.
Entry and exit authenticated observations show unchanged public main and the
annotated v0.9.0 tag, with no visible v0.10.0 tag or GitHub Release. The release
observation includes visible drafts and makes no claim about inaccessible objects.

## Terminal review disposition

Disposition: **amend**. The terminal producer accepts the three advisory findings
with bounded corrections and preserves the remaining phase changes.

The selection helper now uses an explicit per-version supplement table and fails
closed on malformed or unassigned versions. The public checker already resolves
and rejects unsupported versions before this helper; the correction also closes
the helper's independent contract and prevents future implicit fallback.

The v0.10 table declares exactly the two supplemental integration files. Inventory
selection must equal the audited Python set plus those supplements. Missing or
additional supplements fail, making the previously documented divergence
mechanically visible. This amends the suggested repair: schema-1
`required_test_entrypoints` remains the audited minimum, while the code-owned
supplement table declares its exact inventory extension. Historical tuples and
all existing public surface fingerprints remain unchanged. No new deselection is
introduced. The workflow's historical rejection cases now state their assumption
of monotonically increasing release versions.

New helper regressions failed before correction for unknown/malformed identities
and missing/foreign supplemental files. Terminal qualification is recorded in the
closeout evidence; producer candidate snapshots remain distinct from terminal
source bytes. No additional substantive review was obtained.

## Terminal source qualification

The final canonical `bin/apg-test unit-integration` run passes 3,812 unit tests
and 730 integration tests with two integration skips. All eight workers have
identical complete collection in each suite: 112 unit files and 41 integration
files. Both hidden workflow files execute, with all three unit nodes and all
21 integration nodes passing.

| Coverage gate | Statements covered/total | Branches covered/total |
| --- | --- | --- |
| Unit | 10,245/11,931 | 3,726/4,632 |
| Integration | 10,248/11,931 | 3,711/4,632 |
| Combined union | 10,815/11,931 | 4,039/4,632 |

Thresholds, exclusions, coverage configuration and source inventory remain
unchanged. Focused release-checker/workflow qualification passes 108 tests and
219 subtests; runner/private-oracle qualification passes 170 tests. All ten
audited historical policy surfaces retain their recorded fingerprints and exact
selection behavior. Skill-library validation and Python compilation pass.
The retained raw stdout/stderr digests for all 60 producer commands were verified.

The terminal checker passed to natural completion against both preserved producer
A/B diagnostic snapshots, including confidentiality. Each reports 4,444 passes,
91 skips, seven existing deselections and 1,547 passing subtests. Their identities
and manifests remain identical, and both candidate repositories remain clean. Those snapshots predate the terminal helper, unit-test
and comment edits; they are not represented as newly frozen terminal source.
The final-source gate above qualifies those edits. The separately authorized
next freeze must rebuild from finalized APG136. Publication/package wording
qualification passes 66 tests. Final record-identity and changed-public-file
confidentiality checks pass. A fresh authenticated observation confirms unchanged
public main and the annotated v0.9.0 tag, with no visible v0.10.0 tag or Release.

## Disposition and stop

The dispatcher supplied the completed pre-final review. The terminal producer
owns this disposition and task-scoped verification; the dispatcher owns
development Git finalization. No staging, commit or push is performed here.
The next separately authorized freeze must rebuild the entire candidate and
bundle from the finalized APG136 development commit. No diagnostic candidate
is reusable as the release. No final assets, publication packet, public refs,
registry operation, deployment or automatic successor is authorized here.

See [exit 00181](../status/2026/09/10/00181-apg136-v0100-release-harness-source-correction-exit.md).
