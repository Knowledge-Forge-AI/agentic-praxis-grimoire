# APG79E JavaScript Report-Binding Debt and Provisional Integration

## Status

Complete. APG79E accepts `JS-QD-005` as one exact Medium qualification
limitation for provisional integration only, advances ADR 0045 to Accepted with
amendment, and provisionally integrates `javascript-language-profile` with
known qualification debt. It accepts zero Critical or High findings and no
semantic, normative-source, target, owner, host-result, release, rollback,
Test262-rights, or Test262-corpus debt.

## Decision

The maintained historical-report-rewrite mutation changes only the compact
Test262 source-role disposition. It does not directly bind the APG79B managed-
report path, record IDs, or full-file digest. The report itself is currently
byte-exact at SHA-256
`9b56d503039c2907d371b37b72451b6e0b71cca41aa0cd23c074453229698827`.
Both Git-show records regenerate from their immutable commits, both operational
records have exact associations, and a scratch-only one-byte negative fails the
direct full-file comparison.

The operating boundary is narrow. Managed predecessor reports are immutable
historical evidence and are not product inputs or semantic authority. Before
integration and after a material source-role, report generator, report format,
report identity, or managed-report change, maintainers must directly locate and
parse the canonical report through EOF, regenerate its Git-show payloads,
verify operational associations, and compare its full-file digest. This does
not repair the maintained proxy or create a report API.

## Preserved state

The canonical register contains the unchanged five CSS entries, unchanged
`JS-QD-001` through `JS-QD-004`, and new `JS-QD-005`: nine Medium and one Low,
with no Critical or High acceptance. All five JavaScript items block stable
maturity. The APG79D Test262 source-role correction is unchanged, Test262
remains non-normative rights-only evidence with no corpus or oracle use, and
JavaScript structural policy remains deferred.

The resulting development state is 32 canonical skills, 32 catalog rows, and
32 relative projections; 14 skills are stable and 18 are provisional. Routing
contains 30 general routes, one ChatGPT-local route, and 31 checked edges. CSS,
TypeScript, and Markdown remain provisional. Current-development release,
historical corrected v0.4.0, public/active preservation, and disposable
rollback gates passed. Rollback restores 31/31/31, 14/17, and 29/1/30 while
deactivating only the five current JavaScript debt entries and preserving all
five CSS entries.

No target command, Test262 test, publication, deployment, stable-maturity,
Node.js, JSX, React, MDX, Astro-language, Vitest, APG80, or successor work has
begun.
