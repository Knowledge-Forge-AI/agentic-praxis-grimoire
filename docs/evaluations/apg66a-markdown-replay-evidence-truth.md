# APG66A Markdown Replay-Evidence Truth

Date: 2026-08-01

## Result

Complete — APG66 semantic acceptance is preserved while maintained automation
is narrowed to exact register-fixture binding, mechanical navigation, and
mutation-proven targeted guards. The Markdown candidate, ADR 0038 decision,
integration state, public and active v0.4.0, and targets are unchanged.

## Accepted input

APG66 commit `02e4bf6816b4fa532b0e3616cfe5ef070c89e586` and its sole
APG65 parent were reverified with exact local/fetched branch equality and
complete managed report records. APG66 remains Accepted with amendment: its
one semantic correction and two fresh non-author review lanes, including the
34/34 human semantic replay, are not reopened or reinterpreted.

## Reproduced evidence defects

E1 reproduced seven false proofs: changing owner, selection, response, route,
non-owner, structural-signal, or semantic-signal fields made the copied
`expected_consequence` follow the fixture while the candidate was unchanged.
The copied result and every fixture-to-itself assertion are removed.

E2 reproduced mapped-clause false passes for fence ownership/selection/route,
ordinary and evasive raw HTML routes, decomposition signals, normative
contradiction, owner-as-non-owner text, source-boundary drift, and an
unconditional rollback on a no-rollback row. Unrelated candidate text can no
longer satisfy a local machine claim.

E3 adds positive, direct mutation-negative, and global-only near-miss controls
for register identity/projection, clause and coverage closure, local evidence,
and every named targeted guard. Direct contradiction, unconditional-rollback,
numeric-band, and process-leak controls use an explicit bounded lexical grammar;
they do not claim arbitrary-prose interpretation.

E4 reproduced acceptance of an external final normative-spec symlink and a
file beneath a symlinked `docs/specs` ancestor. The exception now requires the
exact skill-specific lexical path, direct ordinary `docs` and `docs/specs`
directories, and a direct ordinary in-repository specification file. Direct,
missing, wrong-skill, external-final-symlink, symlinked-ancestor, and dangling
controls all reach their required dispositions.

Clean public-candidate replay also exposed an inherited temporary-root
collision: configured release validation placed pytest repositories beneath
the root reserved for isolated workers. Its validation environment now creates
a dedicated pytest sibling root, preserving the worker containment guard while
allowing nested runner evidence to complete.

The same clean-candidate replay reached the generic local-path scan and exposed
literal rule examples inside public test-support owners. Those owners now
construct the same runtime markers without embedding forbidden path prefixes in
their own public bytes; focused controls preserve the rejection semantics.

## Truthful proof model

Layer A calculates and verifies the exact accepted APG64 register Git blob from
a public-safe byte-identical source fixture, then parses 36 contiguous rows
under the closed fourteen-field schema. Layer B mechanically projects rows
001–034 into the complete consequence fixture and keeps process rows 035–036
out. Layer C verifies exact clause/coverage closure, mechanical mapped
navigation, locally encoded source/signal/rollback evidence, and the named
APG66/high-risk guards with mutation negatives.

This automation does not claim complete natural-language semantic equivalence
for all 34 scenarios. That authority remains APG66's fresh human corrected-state
review bound to the accepted candidate hashes and patch evidence.

## Preserved state

`markdown-language-profile` remains retained provisional. Development remains
29 canonical skills, 29 catalog rows, 29 projections, 14 stable and 15
provisional rows, 27 general routes, one ChatGPT-local route, and 28 checked
edges. ADR 0037 and ADR 0038 remain Accepted with amendment. CSS remains absent
and ADRs 0031, 0034, 0035, and 0036 remain Rejected. Historical corrected-v0.4
reconstruction excludes the APG66A owners and retains its exact surface.

## Verification and boundary

Focused register, mutation, Markdown unit/integration, link-containment, and
release controls pass. The complete pinned unit, integration, combined, Bats,
coverage, lifecycle, release, identity, link, JSON, privacy, rights, structure,
change-size, diff, delivery, and fetched-equality gates are the terminal
evidence recorded by this phase. No target command ran. Public and active
corrected v0.4.0 did not change.

APG67 JavaScript architecture is recommended in the publication-excluded
handoff but not begun. No readiness, publication, deployment, APG67, or
successor is authorized.

## APG66B forward correction

APG66A remains accepted. APG66B retains its register/blob identity, exact row
projection, navigation, containment, and release results while narrowing later-
reproduced machine overreach: vocabulary is now exact rather than membership-
closed, closed tokens require lexical boundaries, source markers reject direct
polarity contradictions, rows 032 and 034 keep their rollback distinction under
human review, and descriptive or negative counts are no longer mistaken for
normative numeric policy. Markdown candidate semantics and ADR decisions are
unchanged.
