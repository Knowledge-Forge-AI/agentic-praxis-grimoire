# APG77D CSS Known Debt and Provisional Integration

## Scope and authority

APG77D is the human-decision and product-integration phase authorized after the
APG77C repair checkpoint. It preserves the APG76 candidate, every APG77 through
APG77C correction and checkpoint, Claude authorship, accepted ADR 0042, and
corrected historical/public/active v0.4.0. It does not reopen CSS semantics or
TARGET-007, add another oracle lane, claim stable maturity, execute a target,
or begin APG78 or successor work.

## Human-accepted qualification debt

The human product owner accepts exactly `CSS-QD-001` through `CSS-QD-005` for
provisional integration: four Medium qualification-machinery limitations and
one Low compact-schema redundancy. The current canonical register is
[`language-profile-known-debt.json`](../governance/language-profile-known-debt.json),
with its public-safe explanation in
[`language-profile-known-debt.md`](../governance/language-profile-known-debt.md).
The repository consumer rejects duplicate IDs, schema or type drift, status
drift, and any set other than those five exact entries.

No Critical or High debt is accepted. No semantic, source-fact, target,
ownership, runtime, release, or rollback debt is accepted. The current CSS
semantic results and exact target facts remain unchanged. Each Medium item
blocks stable maturity until repair or separate human re-evaluation; none
blocks provisional integration under this exact decision. The Low redundancy
does not block stable maturity by itself.

## Proportionality and proof boundary

The decision commit recomputed the complete then-current machine-readable
aggregate, including the fixture manifest omitted by the APG77C estimate, as
161,704 bytes across four exact inputs. The APG77D integration updates only the
fixture's repository lifecycle fields and its compact-v3 source-binding digest;
the terminal aggregate is therefore 161,893 bytes, or 4.482832 times the
36,114-byte maintained scenario fixture. The four inputs comprise two
publication-excluded qualification records, the maintained scenario fixture,
and the target-first fixture manifest. Exact input identities and digests stay
in the publication-excluded proportionality record.

The aggregate remains below the 350 KiB total, 150 KiB per-artifact, and
120 KiB compact limits. Compact v3 is supporting qualification evidence only.
It is not sufficient semantic, source, disagreement, route, lifecycle,
release, or rollback authority.

## Repository-bound integration

The live tree, rather than caller-supplied lifecycle labels, binds the terminal
state. ADR 0044 is Accepted with amendment. The leaf, specification, coverage,
fixture README and manifest, known-debt owner, catalog, relative projection,
provisional maturity row, router edge, project skill set, release surfaces,
historical exclusions, and test inventory agree on
`provisionally-integrated-with-known-debt`.

Development is 31 canonical skills, 31 catalog rows, and 31 relative
projections: fourteen stable and seventeen provisional. Routing is twenty-nine
general entries plus one ChatGPT-local entry, for thirty checked edges.
TypeScript and Markdown remain provisional. CSS structural policy remains
deferred.

The current development release owns the CSS leaf, projection, public
specification, navigation coverage, target-first fixture, maintained semantic
scenarios, current support and test owners, accepted ADR, known-debt owner,
and APG76 through APG77D public phase records. Every APG76 through APG77D
current-only owner is excluded from corrected historical v0.4 reconstruction.
Published and active corrected v0.4.0 remain byte-identical and unchanged.

## Rollback and verification

The disposable rollback reconstructs exact APG75A pre-integration owners,
overlays the preserved ADR 0044 and APG77D historical decision records, and
performs no reset or history rewrite. It restores 30/30/30, fourteen stable and
sixteen provisional rows, and 28 general plus one ChatGPT-local route. It
removes current CSS skill, projection, catalog, route, project, release, test,
and known-debt ownership while preserving all phase objects and decisions,
TypeScript and Markdown, rejected history, and corrected v0.4.0.

The terminal regression result, immutable review disposition, and delivery
identities are finalized in the APG77D managed report pair and exit record
before the integration commit is adopted.

## Disposition

ADR 0044 is Accepted with amendment and `css-language-profile` is retained
provisionally with exact human-accepted qualification debt. Stable maturity is
not claimed. APG78 is recommended only as the next separately authorized
candidate phase; no APG78 or successor work begins here.
