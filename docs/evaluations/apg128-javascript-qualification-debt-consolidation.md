# APG128 JavaScript qualification-debt consolidation

## Status and authority

APG128 and exit 00173 were available and are allocated to this bounded
qualification-maintenance slice. Terminal closeout retains qualification as
`V0100_JAVASCRIPT_QUALIFICATION_DEBT_SLICE_QUALIFIED`. Dispatcher pre-final review completed with advisory findings. Closeout amends
the evidence claims and records limitations; Git finalization remains dispatcher-owned.

The original scope is exactly JS-QD-001 through JS-QD-004. It adds no language
semantics, skill leaf or maturity promotion. JS-QD-005 and all CSS debt remain
unchanged. Historical APG79B/C/E records remain immutable.

## Proposal and plan-review disposition

Producer proposal disposition: **amend**. The proposal and advisory review are
evidence for the original task, not replacement authority. No cumulative Git
delta was observed at entry. Reported Serena cache changes were operational
metadata, not product changes.

All six plan-review advisory findings were incorporated: JavaScript raw custody includes identity probes
and post-binding failures; the static proof names its runner subset and shared
Node callers; current Node specification section 8 is the CommonJS amendment
owner; CSS subset metrics stay unchanged; historical APG79C summary digests bind
historical content rather than the current register; and pytest-local fixtures
inject sentinels through the process wrapper without manufacturing caller leaks.

Disjoint implementation scopes cover stream custody, fixture/seam controls
and register migration. The parent owns the process guard, integration and
fresh resulting-source qualification. Worker results do not satisfy either
dispatcher checkpoint.

## Qualification boundaries

CommonJS host behavior remains Node-owned, while bounded ECMAScript decisions
remain JavaScript-owned. Availability of the Node leaf does not establish
missing runtime facts for either retained CommonJS artifact.

The process guard is a conservative static proof over its named qualification
files and explicit runner functions. Ordinary and annotated name assignments,
module/callable alias chains, imported aliases, attribute calls, literal
`__import__` and literal `getattr` are covered. Unknown reassignments do not erase
known aliases. Arbitrary reflection, computed attributes and general Python
metaprogramming are outside this proof and remain subject to source review.
General test-runner process use outside the named JavaScript/Node functions is
not represented as JavaScript-owned execution.

Python changes retain the existing large runner and contract owners under this
task's bounded repair authority. Narrow helpers isolate raw custody; no unrelated
module decomposition, dependency or threshold change is included. Reversal is
a coherent rollback of the qualification repairs and current-register migration,
preserving historical accepted-debt evidence.

## Debt dispositions and reproduction

| Debt | Reproduced consequence and qualified disposition |
| --- | --- |
| JS-QD-001 | Baseline accepted changed nonempty evidence/route vectors for both APG78-FX-011 and APG78-FX-014. Exact manifest/artifact/scenario/observation bindings now reject independent and coordinated owner, context, role, stop, evidence, route, source and completion mutations. Current Node CommonJS scenarios bind the host owner; JavaScript remains selected only for bounded language decisions. Superseded under the integrated Node owner. |
| JS-QD-002 | Baseline invocation retained synthetic raw streams in escaping locals and JSON exception context. JavaScript identity probes and JavaScript semantic invocation now consume raw streams and process/parser exceptions before raising bounded diagnostics in outer frames. For those JavaScript owners, exception graphs, all escaping locals and real pytest local rendering are checked; real Node failures cover status, stderr, malformed/invalid JSON, decoding and timeout. Synthetic process-start, interruption and parser-exhaustion injections are separately identified. Repaired. |
| JS-QD-003 | Baseline accepted annotated callable aliases and module rebinding. The named static source/owner set now propagates supported aliases and requires exactly the approved invocation, with rejecting controls for additional wrapper calls and retained forms. Repaired within the explicit AST boundary. |
| JS-QD-004 | The baseline validator already rejected all four wrong scalar values; top-level-await already had a dedicated negative. Four named positive/root-only same-type wrong-value controls now independently fail when root equality is deliberately removed. Repaired qualification coverage, without claiming a previously permissive validator. |

The four scalar pairs are `ReferenceError`/`TypeError` for module-cycle,
integer `1`/`2` for top-level-await, `Error`/`TypeError` for dynamic-import-failure,
and `nearest package.json "type" field`/`file extension alone` for module-goal
evidence. Host observations remain distinct from ECMAScript authority.

Current register revision v2 retains six active entries and four original-entry
digest-bound resolutions. CSS subset metrics remain five debts, one low and four
medium. Historical APG79C summary identity is reconstructed from frozen original
objects; APG79B/C/E bytes are preserved. JS-QD-005 remains unchanged, active,
condition-triggered and stable-blocking. The current backlog has zero
`V010_READY_NOW` implementation items; all other classifications are preserved.
Topology remains 45 leaves, 14 stable / 31 provisional, and 11,142 description
bytes under the unchanged 11,507-byte ceiling.

## Verification

The source-binding corrected focused run passed 440 tests, including current
JavaScript and Node qualification, the debt register and historical retention.
The retained run labelled `focused-all-corrected` is the qualifying focused
record; the earlier `focused-all-final` label does not bind final executable
source. The qualifying combined record is `canonical-unit-integration-3`.
Both nested-runner regression checks passed. The canonical `bin/apg-test
unit-integration` gate passed with 3,716 unit tests and 698 integration tests
(two maintained skips). Exact coverage counts are:

- `PASS unit: statements 10092/11776; branches 3655/4554`
- `PASS integration: statements 10100/11776; branches 3646/4554`
- `PASS combined union: statements 10650/11776; branches 3959/4554`

Thresholds remain unit 80% statements/80% branches, integration 80%/80%,
and union 85%/85%. Skill-library, discovery/context, record-identity,
current-register preservation and whitespace checks pass. The maintained
inventory and release inclusion are unchanged; canonical integration includes
package/release boundary tests. No Go source changed, so no additional Go
test/vet/race run was required by this Python gate.

Integration branch coverage exceeds the 3,644-branch minimum by only two
branches. This remains a fragile qualification margin for integrated readiness.

An initial preflight rejected scratch directory modes; correcting only those
owned modes admitted the run. The first full run passed its unit component
but failed two nested-runner integration tests: new pytest rendering calls
inherited the outer temporary directory and removed it. Each now selects a
distinct child directory. Fresh focused and canonical runs bind the corrected
source. The failed attempts remain evidence, not passing qualification.

Complete sanitized logs, original/retained log digests, source manifests,
pre-correction observations and original-entry resolution witnesses are
retained as phase evidence. No executable source changed during the passing
canonical run. Subsequent current-document reconciliation receives scoped
checks. Closeout confirms the retained logs and source bindings and reruns
record-identity, skill-library and whitespace checks for the terminal documents.
No executable or test-affecting bytes changed after independent review, so
the focused suites and canonical combined gate were confirmed, not rerun.

## Limitations and next boundary

JavaScript remains provisional: JS-QD-005 still blocks stable maturity, and
leaf-specific repeated-use, non-trigger, defect, rollback and non-author
evidence is still required. APG127's delimiter-heavy verifier responsiveness
and cancellation limitation and APG126's browser unknown-family/family-table
fallback limitation belong to integrated readiness, not this implementation.
No successor dispatch, external mutation, host activation or public v0.10
publication is authorized.

## Terminal work-review disposition

Disposition: **amend**. The four debt resolutions and all prior implementation
changes are retained. The independent work review found no qualification blocker;
closeout narrows one overbroad claim and clarifies the qualifying evidence.

1. The shared Node-profile identity probe retains raw process results in an
   inner frame reachable through suppressed exception context. Suppression
   prevents ordinary chained rendering but does not detach that context.
   JavaScript exception-graph custody proof does not cover this Node probe.
   This is an accepted limitation for integrated readiness, not a claim that
   the Node probe has been repaired. The scoped JS-QD-002 resolution stands.
2. The corrected focused run and third combined run bind terminal executable
   bytes. Earlier run names remain historical; qualification witnesses already
   selected the corrected focused record, and the narrative now makes that
   selection explicit.
3. Optional negatives for annotated `subprocess.run`, unknown alias reassignment,
   and asyncio/multiprocessing from-imports are deferred. Required mutation
   categories remain qualified. The named-function guard cannot establish
   ownership for a newly added spawning function outside that set. Carry this
   boundary and the two-branch integration margin into integrated readiness.

No additional substantive review was requested or simulated. These are terminal
producer dispositions of dispatcher-provided findings, not independent acceptance
or successor authority.
