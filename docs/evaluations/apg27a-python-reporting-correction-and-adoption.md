# APG27A Python Reporting Correction and Adoption

Phase ID: `APG27A`

## Outcome

Complete. APG27A corrects the two known defects in the preserved APG27
worktree, adopts the dependency-free Python report core, and preserves APG27
as a truthful partial phase.

## Preserved starting state

APG27 stopped before commit with a working report candidate and two unresolved
acceptance defects: immutable v0.3.0 release-policy reconstruction and a Red
fallback branch-count signal in `validate_source_path`. APG27A began from that
worktree without reset, cleanup, restore, or stash. Its report package,
Git-diff command, tests, ADR 0023, public records, private evidence, and managed
Git-diff and associated operational records were retained.

## Historical release-policy correction

The release-policy adapter now selects an audited surface by semantic version.
The v0.2.0 and v0.3.0 policies are immutable historical inventories; v0.4.0 is
the current development inventory. Unsupported and malformed identities fail
closed. Historical v0.3.0 requires its nineteen skills, nineteen catalog rows,
nineteen projections, fourteen stable rows, five provisional rows, eighteen
non-router routes, and exact release owners without accepting APG27 report-core
additions.

Focused tests cover the v0.2, v0.3, and current development surfaces, missing
current owners, unexpected historical owners, immutable historical arrays, and
unsupported identities. A reconstructed v0.3.0 source and candidate are
validated outside the active and public checkouts.

## Python-profile correction

`validate_source_path` is decomposed into cohesive lexical, location,
destination, metadata, POSIX, and Windows helpers. Its diagnostic order and
observable behavior remain unchanged. Direct tests cover the public function,
success, aliasing, missing and invalid sources, ownership and link invariants,
and the Windows unsupported-safety families.

The maintained report package has no fallback Red structural signal. The
largest module and callable are Yellow; the remaining maxima are explicit
Yellow or Orange compatibility and safety logic.

## Adopted reporting behavior

The importable standard-library package under `libexec/agent_report` is the
current owner for Git-show format version 2, compatible standalone operational-
report version 1 records, Git-diff format version 1 using an invocation-owned
private index, exact same-report Git/operational association, and bounded path,
file, locking, replacement, and drift safety.

The executable `bin/` interfaces are thin Python entry points. GitPython
remains unselected. Windows report replacement and network-filesystem writes
remain unsupported. The former Bash implementation remains available through
Git history as the rollback source.

## Validation and review

The corrected tree passes focused report unit, real-repository integration,
exact legacy-parity, and all 23 retained report-tool Bats contracts. Release-
policy unit, integration, version-policy, and reconstructed historical-
candidate gates cover the corrected policy boundary. Record identity, skill
library, compilation, launcher syntax and help, documentation, privacy, and
whitespace checks are phase acceptance gates.

Fresh Python-profile and release-policy reviewers accept their components after
one bounded correction in each review lane. The Python correction changed test
evidence only. The release correction froze four historical v0.3 arrays and
added an independence regression. A fresh complete-diff reviewer owns the
stable-tree disposition.

## Preserved boundaries

APG27A changes no skill, catalog row, maturity decision, pytest dependency,
test-path layout, ChatGPT topology, personal skill, target repository, active
v0.3.0 integration, public release object, or release tag. It does not claim
readiness, publication, Windows write parity, or network-filesystem support.
