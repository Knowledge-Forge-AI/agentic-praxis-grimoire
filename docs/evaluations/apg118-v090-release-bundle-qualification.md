# APG118 v0.9.0 release bundle qualification

## Scope and status

APG118 performs fresh release qualification from the accepted APG116 product
source. APG117 remains the preserved, blocked prerequisite attempt. This record
is reporting after the frozen product source and is not retroactively included
in the release candidate.

Work-stage disposition: `V090_SOURCE_AMENDMENT_REQUIRES_QUALIFICATION`.
The work candidate is pending dispatcher-owned pre-final review and closeout.
No public publication or host activation has occurred in this phase.

## Construction and package evidence

Two isolated reconstructions produced the same single-parent public successor
of v0.8.1, including its commit, tree, annotated tag, and projection inventory.
Each reconstruction separately built all three Go targets and the complete
ten-asset Python/npm distribution set. Every corresponding asset is byte-equal.
The maintained release epoch and canonical author were used consistently.

Prepared metadata, all three wheel metadata records, and the actual source
distribution metadata contain the full approved UTF-8 Markdown description.
Strict package validation and explicit PyPA Markdown rendering passed. All four
npm tarballs were inspected, including the launcher's three exact-version native
dependencies. A bounded rendered-document audit checked 433 local links and
anchors, including future release URLs against the candidate tree.

The native wheel, a wheel rebuilt from the final source distribution, and the
local npm installation have identical binary and command identities. The rebuilt
wheel is byte-identical to the release wheel. CLI and footprint positive and
negative checks passed across all three installed consumers.

The canonical local Go module contains 1,051 regular files matching the public
candidate and omits 39 symlinks under the maintained Go archive rules. New module
sums were computed. Both external and XO consumers passed ordinary and race
tests against the local unpublished module. Separate released v0.8.1 controls
passed with authenticated checksum verification enabled.

## Verification and operator packet

Fresh policy and canonical unit/integration gates passed: 3,440 unit tests and
621 integration tests, with two historical public-root inputs skipped. Component
statement/branch thresholds remain 80% and combined thresholds remain 85%. Go
tests, race checks, vet, and four marked README examples passed. Scoped
publication regressions passed 78 tests. The complete public check reached and failed its maintained confidentiality
gate because the APG113 public exit contained a private absolute evidence path.
That path is removed in a bounded source amendment without changing the
historical result. The maintained validator regression and corrected projection
scan pass; the amended source has not been reconstructed or release-qualified.

The private operator suite passed 164 tests and 78 subtests against inert
transports. The real check-candidate stopped at the source defect. The packet
now fails closed on pending source qualification. Earlier packet-local passes
and all paired artifacts remain evidence of APG116 only, not final release
acceptance. Live resume also requires operator-owned npm authentication.

Read-only observations verified the full v0.8.1 GitHub, PyPI, and npm predecessor,
the preserved v0.8.0 Git/Go-only history, and historical public Git tags. Raw
transparency-response digests are diagnostic; semantic sums own module-content
comparison.

## Closeout verification

Scoped closeout checks passed 169 tests and 78 subtests. One unchanged full
old-candidate asset-rebuild test was explicitly deselected; corrected-source
qualification was not run. Shell syntax and record identity passed. Both real
packet and candidate preflights refused pending source qualification as expected.
All twenty retained asset copies matched their recorded hashes, and 165 links in
changed public documentation resolved. These checks do not qualify the corrected
source or supersede the historical rendering and consumer receipts.

## Limits and next boundary

Execution evidence is native Darwin arm64. Linux artifacts are qualified
cross-builds, not Linux runtime execution or Linux developer-host qualification.
Future release URLs were checked locally, not reported as successful live URLs.
Operator authentication and live execution readiness remain distinct from local
packet qualification.

Dispatcher pre-final review confirmed the source-amendment disposition with
advisory findings. Closeout repairs the status index, adds explicit preflight
prerequisite diagnostics, and cross-references superseded attempts. Shared Go
build-cache reuse remains a disclosed limitation; corrected-source paired
qualification must use disjoint compiler caches. The dispatcher owns development
Git finalization.
Public execution requires separate operator authorization. No public ref, tag,
release, registry, workflow, repository metadata, host, or JACA mutation is
authorized by this qualification result. No successor phase is started.
