# APG124 v0.10 Toolchain evaluation

## Status and authority

APG124 is the bounded Toolchain slice. Exit 00169 was available and is allocated
to this phase. The dispatcher supplied the independent pre-final findings;
closeout applies the terminal corrections and verifies resulting source.
The dispatcher retains Git finalization. No successor is authorized.

Proposal disposition: **amend**. Original task scope remains controlling;
the exact bound proposal and advisory review are separate evidence. No stage
deltas were supplied at producer entry. The amendment freezes all 42 descriptions, makes owned
npm provisioning explicit, avoids an unnecessary registry service, requires
explicit fail-closed prerequisites, executes the private oracle, preserves
failure evidence before cleanup, supersedes historical architecture additively,
and records native binding provenance.

## Implementation

The [Toolchain architecture](../architecture/v0-10-toolchain.md) owns the
current composition. Provisional Vite and npm profiles join the previous 42
leaves under `v0.10-toolchain`: 44 canonical/catalog/projection/discoverable
leaves and 14 stable / 30 provisional. Each new description is at most 330
UTF-8 bytes (Vite 223; npm 323); actual combined descriptions are 10,819,
leaving 358 bytes under the 11,177 ceiling. All prior descriptions remain
byte-identical. The overall reservation stays 11,507, with only browser-runtime
reserved and unadmitted. Historical policy meanings remain intact.

The private Python oracle now loads its actual retained implementation and
checks the selected current policy, exact identity set, independent canonical
metadata reconstruction and exact headroom. Its obsolete public import failed
collection before correction; the initial corrected run passed 20 tests.
It remains outside the canonical maintained roots and is invoked explicitly.

The standalone browser runner requires owned scratch. Failure diagnostics retain
the initiating error class, sanitized message and bounded cause before supervisor
cleanup. Focused custody and failure-survival evidence is recorded separately
from the preserved browser scenario success predicates.

## Reference and dependency evidence

Phase-entry official tagged sources and registry metadata confirm Vite 8.2.2
and npm 12.0.2, both still the registry latest tag. The selected references were
not advanced. Vite uses Rolldown; its Node requirement is ^20.19.0 || >=22.12.0.
npm requires ^22.22.2 || ^24.15.0 || >=26.0.0. The existing qualified Node
22.22.2 runtime satisfies both.

Vite is MIT licensed, npm Artistic-2.0; fixture dependencies and native optional
bindings retain installed notices and lock/integrity evidence. Exact acquisition
is confined to owned external scratch, with lifecycle scripts disabled. These
are authorized test fixtures, not APGR runtime dependencies. Existing local code
cannot replace the actual selected CLI/bundler for semantic qualification.
Rollback removes task-owned installations and both profile admissions together;
there is no global installation or registry publication.

## Consumer evidence and limitations

Fresh read-only capture confirms the consumer is version 0.4.0, requires Node
>=22, has lockfileVersion 3 and no durable packageManager selection. Its build
is TypeScript compilation. It has no direct Vite application dependency, but
locks Vite 8.2.2 transitively through test tooling. Vite application integration
therefore remains future consumer work; synthetic Vite fixtures do not qualify it.

In the disposable capture, explicitly selected npm 12.0.2 passed `ci
--ignore-scripts`, the maintained build, and the maintained manifest test.
The initial post-dogfood readback preserved source and lockfile bytes. A later
read-only observation found four consumer files changed since capture; the
disposable copies of those files still match the capture. The producer issued
no consumer mutation commands, and attribution of the later changes is unknown.
These results qualify the captured npm workflow, not the later consumer state.
Typecheck failed on
consumer source diagnostics (missing union property, unresolved identifier and
implicit-any parameter). The portable brand test passed two cases and failed
one when expected CLI JSON was empty. Those failures are retained; this is
bounded npm evidence, not a clean-consumer qualification claim. No consumer
fix or snapshot update was performed.

## Verification and terminal disposition

Closeout disposition: **amend**. The four actionable pre-final findings are
accepted: the bundles guide now names the five admitted candidates and current
ceiling; the README maturity bullet is 14/30; the private oracle restores exact
44/10,819/358 literals and eight per-profile byte values alongside canonical
cross-checks; and Vite configuration environments and SSR externalization gain
bounded source-derived guidance. Packaged metadata follows the final Vite body.
The consumer-attribution advisory remains an explicit limitation, not proof of
mutation or a claim that the later checkout is unchanged.

The initial closeout combined gate found one missed 40-route assertion in the
manager-router unit test. Two integration runner tests reproduced that same unit
failure in nested runs. The test now requires exactly 42 general routes and
explicit membership of both new profiles. No threshold or runner policy changed.
The initial run reported 3,585 unit passes and 664 integration passes, with two
historical target-dependent skips; its failed status is retained.

Resulting-source work-stage evidence:

- Affected Python files: 679 passed and 650 subtests passed. One existing
  historical APG11 public-v0.1 dogfood test skipped because its separately
  supplied target was absent; no npm or Vite qualification lane skipped.
- Synthetic npm and Vite receipts: 12/12 scenarios each, zero failures/skips.
  Final focused toolchain checks passed 31 tests; the later Vite receipt/privacy
  correction passed its 15-test focused suite and the affected-source run.
- Shared browser maintenance: 36 tests passed, including the maintained
  Playwright/accessibility integrations across Chromium, Firefox and WebKit.
  A final synthetic-path fixture redaction passed all 34 browser unit tests.
- Explicit private oracle: 20 passed. Profile/oracle combined check: 24 passed.
- Go: `go test -race ./...` and `go vet ./...` passed.
- Library: exact 44 canonical/catalog/projection leaves. Context: 44 discoverable
  leaves; metadata reconstructed twice with identical bytes; all 42 entry
  descriptions preserved byte-for-byte.
- Inventory validation, record identity (APG124/00169), package-facing metadata
  checks and `git diff --check` passed.

Initial stale topology assertions, npm local-source script approval mismatch,
missing profile owner headings and Vite receipt path leakage were corrected and
rerun. npm subprocesses enforce offline configuration; Vite subprocesses use an
explicit environment allowlist. Failure history remains evidence, not a pass.
Closeout ran `bin/apg-test unit-integration` on the corrected source: 3,586 unit
tests and 666 integration tests passed, with two historical external-target
checks skipped. Unit coverage passed at 9,931/11,595 statements and 3,563/4,440
branches; integration passed at 9,947/11,595 and 3,553/4,440; the combined union
passed at 10,480/11,595 and 3,861/4,440. Thresholds are unchanged.

All 92 focused profile, toolchain, router, browser-scratch and private-oracle
tests passed after correction. The explicit oracle separately passed 20 tests.
Closeout also passed `go test -race ./...`, `go vet ./...`, library topology,
package corpus verification, repeated byte-identical context reports and record
identity. Both registry references and engines were re-attested unchanged.
All 42 entry descriptions remain byte-identical. Final documentation/evidence
updates receive record and whitespace checks; no executable bytes changed after
the passing combined gate.

Terminal disposition: **amend**. Result: `V0100_TOOLCHAIN_SLICE_QUALIFIED` for the
bounded synthetic toolchain and captured disposable npm workflow. The consumer
failures and later drift attribution remain limitations; this is not a clean
current-consumer claim. No consumer operation was repeated during closeout.
The terminal corrections received task-scoped verification, not another
independent review. Git finalization remains dispatcher-owned. Public v0.9.0,
host/adapters, JACA, registries and browser-runtime implementation remain outside
this slice; no successor is authorized.
