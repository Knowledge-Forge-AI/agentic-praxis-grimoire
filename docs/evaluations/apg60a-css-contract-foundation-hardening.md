# APG60A CSS Contract Foundation Hardening

Subsequent correction: APG60B preserves this accepted result and closes
additional traceability, narrative-state, exact-owner, derived-value,
module-isolation, and future decision-lifecycle false-pass classes. See
[APG60B CSS Traceability and Decision Closure](apg60b-css-traceability-and-decision-closure.md).

Status: Complete — bounded forward correction of the APG60 executable
foundation, with no candidate authoring or successor authorization.

APG60A accepts APG60 as the historical baseline and corrects four narrow
foundation defects found before APG61:

- projected material growth now stops independently of whether the requested
  operation is labeled correction, feature, decomposition, or documentation;
- bounded exceptions preserve their real human-instruction or
  repository-policy grant source and fail closed when the source is missing,
  unsupported, or incidental;
- rejected-current residue detection treats dangling and broken symlinks as
  lexical state while preserving explicit exclusion and historical-evidence
  boundaries; and
- a generic actual-tree retained-surface gate now requires complete,
  non-duplicated, parseable agreement across every declared current owner.

The original 60 case IDs, order, family counts, 300/600/900 bands, counting
rules, exclusions, and semantic-owner boundaries remain unchanged. Schema v2
adds `contract_revision`, `supersedes_contract_sha256`, and an explicit
`exception_grant_source`. Only case `APG60-CSS-024` changes its frozen expected
result: its real owner is `repository-policy`, with the corresponding apply
action.

The actual retained-surface gate proves integration closure, not candidate
prose quality or semantic acceptance. It derives leaf, projection, fixture,
test, release, and inventory expectations from live owners; rejects omitted,
partial, duplicated, malformed, dangling, hard-coded, and contradictory
states; preserves historical public evidence; and uses no private evidence at
runtime.

CSS remains absent. ADR 0036 remains unused. Development remains 28 canonical
skills, 28 catalog rows, and 28 projections. Public and active v0.4.0 remain
unchanged. APG61 is recommended only through the publication-excluded handoff;
it is not authorized here. No target execution, installation mutation,
readiness, publication, deployment, or successor phase occurs.
