# APG81B Node.js integration-contract clarification checkpoint

## Scope

APG81B is a separate human continuation decision after the APG81A terminal
checkpoint. It preserves the accepted controlled-local-or-CI Node
qualification correction and changes no retained Node semantic, source, target,
or harness contract. It clarifies the intended candidate-preserving rollback
shape and reconstructs the discarded ADR-index defect for review.

## Human rollback clarification

APG81A simultaneously required rollback to restore 32 canonical skills and to
preserve the already-authored Node candidate. Its exact pre-integration shape
was 33 canonical skills, 32 catalog rows, and 32 projections. The historical
High remains valid evidence against that literal assignment, but it is not Node
product debt or a current semantic, harness, source, target, release, or
implementation defect.

APG81B establishes that a future successful rollback must remove only current
Node integration owners and retain the candidate, specifications, fixture,
threat model, ADR, phase history, and all ten CSS/JavaScript debt entries. Its
intended count projection is 33/32/32, 14 stable / 18 provisional, and 30
general / one ChatGPT-local / 31 checked routes. The reviewed disposable
rollback reached those counts but did not close retained lifecycle truth, so it
is diagnostic evidence rather than an accepted rollback result.

## ADR-index reconstruction

The discarded APG81A integration detached ADR 0045 from its status adjacency
and represented ADR 0046 as both Accepted and Proposed. The APG81B candidate
reconstructed one adjacency-owned current status for each row and supplied
displacement, duplicate, wrong-status, early-acceptance, and neighboring-status
negatives. Because the complete candidate did not pass staged review, those
integration bytes are not retained. ADR 0045 remains Accepted with amendment;
ADR 0046 remains Proposed.

## Terminal review

The complete staged candidate was frozen and reviewed in five non-author
scopes. Review found zero Critical, zero High, three Medium, and zero Low
material defects with zero Node debt:

- rollback retained an Accepted ADR while the retained candidate leaf and
  specification still claimed current Proposed lifecycle;
- the staged review records remained pending while the proposed commit message
  claimed completed zero-finding review; and
- a sensible xdist timeout-cleanup test repair exceeded APG81B's integration-
  only authority and changed a frozen APG81A test surface.

The integration candidate, including the unauthorized test change, was
discarded. No integration commit exists.

## Result and boundaries

The retained result is a terminal repair checkpoint. ADR 0046 remains Proposed
and `nodejs-runtime-profile` remains corrected, repair-required, and
unintegrated with zero Node debt. The candidate branch remains 33/32/32 while
development main remains exact APG79E at 32/32/32, 14 stable / 18 provisional,
and 30 general / one ChatGPT-local / 31 checked routes. All ten existing CSS and
JavaScript debt entries remain exact.

Corrected historical, public, and active v0.4.0 remain unchanged and both
targets remain unmodified and unexecuted. APG81B authorizes no forward repair,
stable maturity, readiness, publication, deployment, target command, APG82, or
successor work. A new human decision is required.
