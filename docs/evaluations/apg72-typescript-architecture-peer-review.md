# APG72 — TypeScript Architecture Peer Review

## Review state

APG72 normally delivered and independently verified exact APG71, rebuilt the
TypeScript owner and compiler-role oracle, reverified source, package, rights,
documentation, target, and corpus evidence, froze the complete initial
material set, and used the single permitted coherent architecture correction.
Two fresh non-author lanes then found genuinely new material defects. ADR 0041
is Rejected, eligibility is `not-applicable-rejected`, and the corrected
proposal is historical evidence rather than current architecture input.

No TypeScript or JavaScript skill is authored. No catalog, projection,
maturity, route, fixture, maintained test, project, release, public, active, or
target owner changes. APG73 and every successor remain unauthorized.

## Exact APG71 delivery

Exact APG71 commit, tree, sole APG70 parent, 30-path patch, and 2,440 inserted
lines were independently verified. Its canonical managed Git-show regenerated
byte-identically, including the authoritative patch digest. The Claude branch
was normally pushed and refetched while `origin/main` remained exact APG70;
Claude authorship and history were not rewritten.

The APG71 message contains the required four factual sections, but the current
checker rejects their wrapped continuation-line form. The managed report has
one complete Git-show record and no associated operational record. Both are
ordinary immutable-delivery debt under the assignment's explicit report
waiver; neither creates object, parent, patch, authorship, or decision
ambiguity.

## Source, package, rights, and target result

- Exact TypeScript 7.0.2 is the native compiler line. It supplies a native
  command-line package but no supported 7.0 programmatic API. A later API is
  moving expectation only.
- Exact TypeScript 6.0.2 and 6.0.3 are legacy JavaScript compiler releases.
  Exact TypeScript 5.9.3 is independently bound and controls the theme's
  version-specific implementation truth; TypeScript 6 cannot stand in for it.
- The TypeScript 6 compatibility package is a wrapper around an aliased
  dependency. Its own version and missing source binding do not identify the
  compiler it resolves.
- Moving Handbook and TSConfig prose is explanatory evidence. Version-scoped
  release notes speak only for their named release; exact source/package and
  target evidence controls exact results.
- The website has TypeScript 7 present as a peer lock resolution but no exact
  evidence of an exercised compiler role. The theme configures 5.9.3 checker,
  language-service, and declaration-emitter roles, while `tsx` is the separate
  script runner/stripper. Its stale lock proves literal historical resolutions,
  not a reproducible current install graph.
- Compiler and documentation rights were separately reverified. Target
  expression was not copied; one target lacks an established reuse grant and
  the other has qualified rights surfaces, which remain non-blocking under the
  clean-room no-copy method.

Neither target was run or changed. Descriptive corpus measurements remain
breadth evidence only and do not become a numeric structural policy.

## Independent initial review

Two candidate-blind lanes froze complete 22 semantic / 12 boundary / 2 process
vectors before proposal-visible review. Source and target review then closed
the exact 5.9.3 and compiler-role facts. Proposal-visible review reproduced
the complete assigned M1-M18 material family:

- package presence was treated as compiler execution and TypeScript 6 source
  was used to explain exact 5.9.3 behavior;
- compiler family, role, version, selection, state, and evidence were combined
  in one generation token;
- option names lacked exact values, signals lacked independent positive and
  false-positive definitions, and present versus required authority was
  conflated;
- routes mixed evidence, competencies, primary owners, and unresolved
  receivers;
- checked JavaScript lacked a source-language owner, `.mts` and `.cts` were
  collapsed, and emitted JavaScript and generated declarations lacked typed
  coverage;
- role splits, host/TSX composition, moving documentation, decorator regime,
  exact-version stops, and row-specific semantic consequences were incomplete;
  and
- source disposition B and structural disposition D were defensible only
  after narrowing, while initial authoring eligibility was not.

Additional material findings closed two register-only tokens, a free process
owner, exact target-role mistakes, the missing exact 5.9.3 source, and the
compatibility wrapper's unbound resolved compiler. The initial parser proved
the 22/12/2 shape and exposed its own open option-vocabulary limitation; it did
not prove semantics.

## Single coherent correction

The correction treats the initial set as one lost-identity problem. Every
consequence-bearing decision now preserves exact compiler role, family,
version, selection source, role state, evidence state, option facts, source
kind, artifact class, present and required authority, active signal, route
obligation, and rollback without conflation.

- Source disposition B becomes role-selected, claim-specific exact authority.
- Structural disposition D remains deferred; the proposal makes no structural
  judgment and defines no numeric band.
- The contract owns closed vocabularies, a structured compiler-binding grammar,
  exact JSON option facts, typed route obligations, and twelve independent
  signal definitions.
- The semantic register retains 22 stable IDs with 20 exact fields. Stable
  semantic rules are distinguished from exact-version meta-rules, which stop
  when consequence-controlling evidence is missing.
- The boundary register contains 14 rows with 21 exact fields: B013 adds the
  `.cts` outcome and B014 adds generated declarations. Two five-field process
  rows use the generic lifecycle owner outside candidate prose.
- Checked JavaScript, TSX, embedded regions, handwritten and generated
  declarations, emitted JavaScript, compiler-role matrices, module resolution,
  runtime loading, declaration environments, and project graphs retain typed
  adjacent ownership.

The corrected proposal result before fresh review was
`authoring-eligible-with-narrowing`. That terminal-neutral result granted no
candidate or successor authority and did not survive fresh review.

## Corrected-state evidence

Before fresh review, APG72 preserved the actual corrected full-index patch,
exact path and hash set, parser and mutation-negative result, candidate-blind
vector digests, source identities, and reviewer-to-hash bindings in one managed
Git-diff record and its associated operational record. The preserved snapshot
contained 15 paths, 1,807 insertions, and 1,057 deletions. The corrected parser
reported exact 22/14/2 rows, 20/21/5 fields, zero baseline errors, and 24/24
seeded mutations rejected. Those mechanical facts did not prove semantics.

## Fresh review and rejection

Both fresh lanes independently replayed all 22 semantic, 14 boundary, and 2
process rows against the exact corrected hashes and rechecked M1-M18 plus
A1-A5. They independently found that B005 and B006 pair the bounded TypeScript
handoff with `effective-options`, an obligation owned by project configuration,
so the closed route grammar cannot truthfully express the intended static
analysis route. Both also found B008 asserting programmatic API, editor, and
embedded roles as configured even though its input establishes only the native
CLI plus an editor-or-embedded disjunction and no API use.

Fresh lane A additionally found that the closed source-kind model collapses
handwritten and generated `.d.ts`, `.d.mts`, and `.d.cts` distinctions, and
that B001 records input-present compiler/config evidence as still required.
Fresh lane B found B006 routing an editor obligation absent from its input and
the target conclusion upgrading an invoked checker stack to an editor role.
These are genuinely new material owner, route-obligation, role-state,
source-kind, and evidence-state defects after the sole correction.

The assignment forbids a second semantic correction. ADR 0041 is Rejected,
eligibility is `not-applicable-rejected`, and the corrected architecture,
contract, and registers remain historical evidence only. Source disposition B
and structural disposition D remain defensible in isolation but do not rescue
the rejected design. APG73 is not recommended or begun.

Development remains 29 canonical skills / 29 catalog rows / 29 projections,
14 stable / 15 provisional, and 27 general / 1 ChatGPT-local / 28 checked
routes. Markdown remains retained provisional. CSS and JavaScript skills remain
absent; their rejected ADRs remain unchanged. Corrected public and active
v0.4.0 and both read-only targets remain unchanged.
