# APG79B JavaScript contract and harness closure

APG79B exercises the separate human continuation authorized after the APG79A
repair checkpoint. It preserves APG78, every APG79 round, and both APG79A
objects while attempting to correct only the five retained Medium findings.
Fresh immutable-correction review found four new Medium defects. No JavaScript
debt is accepted, ADR 0045 remains Proposed, and integration is blocked.

## Corrected contract

The two standalone CommonJS artifacts remain whole-file-owned by the existing
Node/CommonJS owner. JavaScript is `selected` only for the bounded ECMAScript
decision; wrapper bindings, loading, resolution, and execution remain routed
and stopped. `embedded-route` remains reserved for a region nested in a
different host file.

Scenario rows now use their existing classes as closed schemas. Each of the
three target rows requires its exact public-safe commitment. Semantic and
fixture rows forbid that field, and target commitments provide neither source
nor semantic authority.

## Attempted qualification boundary

One repository process-spawn site owns JavaScript engine validation and
invocation. Raw stdout and stderr remain local to that owner, every observation
is checked against a closed output contract, and only redacted structured facts
return. The correction exercised default pytest assertion rendering with
non-sensitive synthetic output.

An AST control closes the named maintained Python import, alias, attribute, and
call forms for process invocation while making no claim about arbitrary dynamic
code. The exact executable is re-resolved and revalidated after invocation;
test-owned path replacement is rejected. The correction added recursive output
mutation evidence over the maintained registry.

## Fresh review

Reviewer A found that the complete CommonJS owner, context, unresolved-role,
stopped-response, evidence, route, and completion vector is not mechanically
closed for both artifacts. Reviewer B and Reviewer C independently found raw
streams reachable through exception traceback locals and chained exceptions,
including pytest rendering with locals. Reviewer D found missing same-type
wrong-value mutations for four root-scalar output contracts. The cross-surface
review confirmed the owner and privacy findings and additionally found static
annotated-alias and module-rebinding process bypasses.

The unique union is 0 Critical, 0 High, 4 Medium, and 0 Low. These are
repairable findings, not accepted debt and not a rejection or removal decision.
APG79B permits no second correction after immutable review.

## Current boundary

The correction passes 134 focused tests, including the prior 110, and preserves
the exact Node and TypeScript prerequisites, 24 semantic purposes, 14 fixture
purposes, three target rows, and all five accepted CSS debt entries. The
candidate is `repair-required-after-apg79b` and unintegrated. Catalog,
projection, maturity, routes, project selection, releases, public and active
v0.4.0, and read-only targets remain unchanged.

The zero-debt gate did not pass. Main remains exact APG77D, no integration owner
changed, and APG80 is not recommended. No target command, full integration
regression, release reconstruction, rollback adoption, stable maturity,
readiness, publication, deployment, or successor work was run or authorized.
