# APG79A JavaScript terminal-proof closure and integration

## Correction state

APG79A exercises the human continuation authorized after APG79. It preserves
the exact APG78 candidate and all three APG79 correction rounds plus the APG79
terminal checkpoint. One bounded correction closes only the four retained
terminal-proof findings:

- whole-file ownership is independent of decision-scoped JavaScript Selection;
- consequence-bearing authority, source binding, lifecycle, provenance,
  completion, and conclusion fields use closed machine-owned vocabularies;
- every maintained semantic or syntax subprocess uses immediate pre- and
  post-invocation evidence for the exact approved JavaScript engine; and
- every material executed fixture result is asserted exactly.

The engine evidence is bounded pre/post identity evidence. It does not claim
continuous executable identity during a subprocess. Explanatory row prose is
not a machine-owned oracle, and target rows retain public-safe commitments
rather than target source.

## Fresh review

Four fresh non-author lanes bound the immutable correction commit, tree, and
artifact hashes. Their union is zero Critical, zero High, five Medium, and zero
Low:

1. Two whole-file CommonJS fixture vectors retain `embedded-route`, although the
   corrected Selection contract reserves that token for a region nested inside
   a different host file.
2. The closed scenario checker still accepts the target-only commitment field on
   ordinary semantic and fixture rows.
3. Actual pytest assertion rewriting can expose captured engine stdout and
   stderr even though the standalone diagnostic helper returns a bounded string.
4. The maintained wrapper-bypass guard misses common process-call aliases and
   therefore does not enforce the claimed helper boundary.
5. Required after-invocation resolved-path replacement and per-output mutation
   evidence is incomplete.

The first two findings were independently reproduced by the cross-surface lane.
The exact engine identity, bounded pre/post wording, material-value equality
assertions, 24 semantic / 14 fixture / three target purposes, and CSS debt
preservation otherwise remained intact.

## Terminal disposition

APG79A accepts no JavaScript debt and has no post-review correction authority.
The candidate is therefore `repair-required-after-apg79a`, ADR 0045 remains
Proposed, and `javascript-language-profile` remains unintegrated. No catalog,
projection, maturity, capability, project, release, or test-inventory owner was
added. Integrated development remains exact APG77D at 31/31/31, 14 stable and
17 provisional, and 29 general plus one ChatGPT-local route. CSS known debt and
provisional CSS, TypeScript, and Markdown are unchanged.

Full integration regression, release reconstruction, rollback adoption, and
mainline movement did not run because the zero-debt review gate failed. No
target command, publication, deployment, stable-maturity change, Node.js
profile, APG80, or successor phase was begun. The next action is a new human
decision.
