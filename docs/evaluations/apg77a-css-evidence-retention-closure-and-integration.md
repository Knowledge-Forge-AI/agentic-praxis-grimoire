# APG77A CSS Evidence-Retention Closure and Integration

## Result

Complete repair checkpoint. APG77A preserves one immutable H1/H2 correction
under explicit human continuation authority, but fresh review finds three High
defects. ADR 0044 remains Proposed, CSS remains `repair-required` and
unintegrated, and a new human decision is required. This is not rejection,
removal, debt acceptance, or integration.

## Narrow correction

H1 retains complete manifest and lockfile SHA-256 identities and every tracked
target path, Git object, mode, byte size, and blob SHA-256 in compact private
evidence. Fresh exact Git object reads reproduce all 17 website and 69 theme
paths. Both target roots are unchanged; neither target was mutated or executed.

H2 retains two independently authored, separately complete 45-purpose vector
sets: 24 semantic, 14 fixture, and seven target purposes. Each row carries the
complete required field set without cross-lane inheritance. Third-lane
adjudication preserves disagreements and traces the unchanged resolved
fixture. The unchanged focused CSS checkpoint passes 23 tests and the new
evidence-retention owner passes 43.

## Fresh review

Three fresh non-author reviewers bound the exact correction, tree, complete
diff, private evidence identities, and unchanged resolved fixture. They found
zero Critical, three High, zero Medium, and zero Low defects, with no accepted
debt:

1. The target-facing SVG `<style>` scenario cites CSS Style Attributes, which
   governs element style attributes, instead of the SVG styling and media-query
   authorities supported by both lanes and preserved evidence.
2. H2 mutation controls can accept a partial route-obligation deletion, a
   content-level target-purpose collapse, and an authority introduced only by
   a blanket adjudication default. They therefore overclaim route, purpose, and
   no-field-from-nowhere closure.
3. One Lane T field copies a contiguous target CSS fragment, violating the
   identity-only and no-target-byte boundary.

The first is a newly exposed source-binding defect outside H1/H2. The latter
two are immutable-correction evidence defects. APG77A does not repair any of
them after fresh review.

## Qualification and disposition

The pinned unit lane passes 2,810 tests. The integration lane passes 565,
skips two, and truthfully fails the same five assertions that require either a
fully integrated current library or live-absent CSS. The combined lane passes
its 2,810-test unit component and reproduces those five integration failures.
Twenty-three configured Bats tests pass. This is not a green full integration
gate.

No catalog row, projection, maturity row, route, project selection, release
owner, or maintained integration owner is added. Development remains 30/30/30,
14 stable / 16 provisional, and 28 general / one ChatGPT-local / 29 checked
routes. TypeScript and Markdown remain provisional; JavaScript is absent.
Corrected historical, published, and active v0.4.0 and both targets remain
unchanged. APG78 is not recommended or begun.

## Boundary

No integration rollback is adopted because integration never opens. No stable
maturity, readiness, publication, deployment, target execution, mainline
adoption, JavaScript-family work, APG78, or successor work occurs. Further CSS
work requires a new human product decision.
