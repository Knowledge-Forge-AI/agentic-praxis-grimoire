# APG119 v0.9.0 corrected-source release qualification

## Scope and source boundary

APG119 qualifies the accepted APG118 source amendment. The product source was
frozen once at phase entry, before this reporting record. Reporting changes do
not redefine that source. APG118's source-amendment outcome and its unqualified
earlier artifacts remain historical evidence; neither is rewritten as a release.

The bound planning proposal is amended to incorporate the five advisory findings:
immutable-source regression execution, explicit supplemental locator inspection,
separate compiler-cache receipts, exact Node version checks, and separate local
qualification and attended execution readiness. Review and Git finalization remain
dispatcher-owned. No public publication has occurred.

## Reconstruction and descriptions

Two independent public candidate constructions produce the same commit, tree,
annotated tag, and sole v0.8.1 parent. Both complete ten-asset bundles were built
through the maintained production owners using separate fresh compiler caches.
All corresponding asset bytes match. Actual archive inventories and distribution
checks confirm names, sizes, hashes, modes, platform roles, licenses, version,
native binary identities, and the launcher's exact native dependency versions.

The rebuilt distribution bytes also match the historical distribution bytes;
this is an observed result of real reconstruction, not artifact reuse. The
corrected public tree and canonical Go module have newly derived identities.
The amended historical status document is outside distribution payloads, so asset
digests alone do not distinguish the source amendment. Construction receipts and
the corrected public Git and module identities carry that binding.
Private receipts remain excluded while required CI and XO surfaces are retained.

The maintained confidentiality validator and its actual negative-control
regression pass against the frozen corrected source. The complete maintained
public-release validator also passes with the pinned developer toolchain.
Supplemental inspection of
projected human documents found only generic product outbox templates; those
templates do not identify private evidence. Validator policy and exclusions are
unchanged.

The initial complete public-validation attempt returned 26 failures, 3,957 passes,
24 skips, and seven maintained deselections under system Python and an indirect
Node entry. A subsequent run with the pinned interpreter and direct tools passed
and supersedes that attempt. The initial source-suite prerequisite refusal and
an owner-test environment-binding failure are also retained with their passing
supersessions. No policy, threshold, assertion, or maintained selection was weakened.

Prepared metadata, all three wheel metadata records, and source-distribution
metadata contain the complete approved UTF-8 Markdown description. Strict package
checking and actual Markdown-to-HTML rendering pass on all four extracted
descriptions. All four npm tarballs have their README and discovery metadata
checked. A rendered-document audit passes 433 local links and anchors, including
future version targets checked against the actual candidate tree.

## Source and consumer evidence

The changed-input matrix identifies documentation consumed by integration and
policy tests. No historical suite receipt is reused as current qualification.
Fresh policy, 3,440 unit tests, and 621 integration tests pass. Two unavailable
historical public-root inputs remain explicitly skipped. Component statement and
branch thresholds remain 80%, and combined thresholds remain 85%. Fresh Go tests,
race tests, vet, and all four marked README examples pass.

The new canonical Go module contains 1,055 regular members that match the
immutable public tree and omits 39 symlinks under the maintained archive rules.
Both disposable prospective external and XO consumers pass ordinary, race, and
verification lanes against the local unpublished proxy. Released v0.8.1 controls
pass with public proxy and checksum authentication retained. Released fixture
requirements and sums remain unchanged.

The native wheel, an actual source-distribution-rebuilt wheel, and local npm
installation have identical native binaries and command results. Developer-runner
tests preserve truthful checkout and pinned-prerequisite refusals. Linux assets
are cross-built and inspected, not executed on Linux.

## Operator verification and current boundary

The retained executor is adapted to the new source, candidate, module, and asset
bindings. It preserves complete predecessor classification, semantic Go checksums,
compatible module-info provenance, Go-first sequencing, durable incremental
readbacks, terminal read-only reconciliation, and attended authentication rules.
Preflight diagnoses missing tools and version failures and creates owned scratch
when absent. Source or release identity does not depend on an old scratch directory.

The adapted owner suite passes 176 tests and 78 subtests without deselection,
including a real absent-artifact reconstruction. Mutating branches use inert
transports or disposable fixtures. The actual packet preflight passes and reports
execution readiness separately. Complete production candidate preflight passes,
including fresh candidate validation, artifact reconstruction and reconciliation,
local Go qualification, installed smoke checks, and released checksum controls.
Resume checking stops at unavailable npm authentication; no authentication is performed.
This is an attended execution prerequisite, not a failed local bundle qualification.

The work-candidate disposition is
`V090_RELEASE_BUNDLE_QUALIFIED_FOR_OPERATOR_PREFLIGHT`. Reporting-only receipt
references and completion wording are rebound after production execution, with
the executed manifest preserved and affected packet/owner checks repeated.

All four internal workers have drained. Their results are corroborating evidence,
not dispatcher review checkpoints. Dispatcher pre-final review supports qualification
with advisory findings and no demonstrated shipping defect. Closeout accepts those
findings and amends reporting for source binding, packet accounting, and failure
supersession visibility. Terminal reporting receives affected verification; it is
not claimed independently reviewed. Live development observations differ from the local development head;
the dispatcher owns development delivery. Operator authentication and publication
require separate attended authority. No public ref, tag, release, registry,
workflow, host, JACA, or successor mutation is authorized by this work.
