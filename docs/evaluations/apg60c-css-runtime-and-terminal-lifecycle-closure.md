# APG60C CSS Runtime and Terminal Lifecycle Closure

<!-- APG-CANDIDATE-STATE: css-language-profile absent -->

Status: Complete — APG60B remains accepted and the pre-authoring CSS
foundation is corrected forward without candidate authoring.

APG60C starts from exact APG60B and closes four independently reproduced
false-pass classes:

- F1 replaces assignment-presence checking with a closed owner-source static
  purity proof, under a declared trusted-interpreter boundary, that requires
  one immutable supported binding and rejects later assignment, deletion,
  augmentation, dynamic replacement, and unproven mapping or collection
  mutation;
- F2 keeps the exact candidate-state marker as the mechanical authority,
  extends bounded direct-contradiction diagnostics, and explicitly retains
  human review for arbitrary prose;
- F3 defines mutually exclusive actual repository states for pre-authoring
  absence, authored Proposed but unintegrated, retained provisional or stable,
  and rejected with public and publication-excluded history preserved; and
- F4 requires each current survivor owner to be a direct regular no-follow
  file with strict UTF-8, duplicate-key-refusing JSON, or Python parsing as
  declared.

The marker proves mechanical current state. The contradiction scan catches
the frozen bounded vocabulary. Human review remains required for arbitrary
prose.

F1 proves that supported owner source cannot replace the protected binding.
It assumes that the validated standard interpreter's import resolution and
hook, signal, tracing, audit, and codec registries are not externally replaced
between validation and use. Host or interpreter tampering is outside this
source contract; the checker does not claim to control arbitrary Python
runtime registries.

The authored-but-unintegrated gate exists before APG61 and requires Proposed
ADR 0036 authoring evidence while keeping all integration surfaces absent and
the integrated inventory at 28/28/28. Terminal rejection cannot pass by
removing its authoring or rejection history, and a current survivor cannot be
replaced by a symlink, malformed Python, or duplicate-key JSON.

The APG60A CSS behavior fixture remains byte-identical at
`1e96535c451dda91dea871679f5a6c9ee7639f4bcd443b4dfbbda4eb28086aed`.
All sixty IDs and expected objects, the 300/600/900 thresholds, counting,
exception authority, projection overrun, one-count rules, exclusions,
semantic owner boundaries, traceability row semantics, clause-marker syntax,
and future ADR 0036 terminal statuses are unchanged.

CSS remains absent. ADR 0031, ADR 0034, and ADR 0035 remain Rejected; ADR 0036
remains unused. Development remains 28 canonical skills, 28 catalog rows, 28
projections, and fourteen stable plus fourteen provisional skills. Corrected
public and active v0.4.0 remain unchanged. The superseding
publication-excluded handoff recommends APG61 but grants no authority to begin
it or any successor.

## Subsequent APG60D correction

APG60C remains accepted. APG60D later replaced the runtime/call-finality
overclaim with truthful static source-binding integrity and extended exact
future phase history through APG60C, APG60D, APG61, and APG62. This bounded
forward note does not alter APG60C's delivered object or disposition.
