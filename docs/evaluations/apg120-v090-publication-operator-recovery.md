# APG120 v0.9.0 publication operator recovery

Phase ID: `APG120`.

## Scope and result

Terminal closeout disposition: `V090_PUBLICATION_RECOVERY_PACKET_QUALIFIED`.
Dispatcher pre-final findings are dispositioned; scoped terminal verification passed.

APG120 repairs a publication-packet metadata contract after v0.9.0 publication
advanced through GitHub, Go, and PyPI. The production reader reproduced a missing
`license_expression` key after verifying four distribution downloads. The
manifest now binds `AGPL-3.0-or-later`; required PyPI metadata is validated before
remote operations, and malformed metadata produces controlled stops.

The recovery authority preserves product source, the public candidate/tag, and
all ten qualified artifact identities. Current packet seals record the correction
explicitly; historical executed manifests, bindings, and available attempt
evidence remain unchanged. The first attempt's overwritten terminal machine
result is unavailable and is not reconstructed.

## Recovery behavior

The forward-only recovery path accepts the published-release prefix and exact
successors, verifies the Python workflow/job/publish step even when PyPI exists,
and publishes only an explicitly authorized missing npm suffix. It cannot replay
Git/tag/Release creation or upload PyPI. Existing terminal npm authentication is
preserved. Separate per-invocation results retain partial receipts and sanitized
operation/key diagnostics.

Read-only live reconciliation found exact Git/tag, published Release notes and
ten assets, successful release-triggered Python publication, exact Go content and
checksum evidence, and four exact PyPI files with the expected SPDX expression
and description. npm v0.9.0 remained absent. The observed prefix is
`exact_published_release_pypi_prefix`; remote state is rechecked before continuation.

## Evidence and boundaries

Focused production tests cover the demonstrated defect and metadata negatives,
workflow identity, each supported npm prefix through simulated completion,
no-ops, drift, partial failures, and terminal authentication behavior. Packet,
seal/inventory, record-identity, and whitespace checks are scoped to this repair.
No public execute path was invoked.

APG119 product qualification is reused for unchanged source and artifact bytes;
its 3,440 unit and 621 integration suite results are not claimed as fresh APG120
execution. No rebuild, consumer installation, public mutation, host activation,
or successor work is performed. Recovery terminal registry readback has a distinct
disposition and does not claim renewed installed-consumer qualification.

The planning proposal is amended to incorporate the advisory findings. Terminal closeout amends the reviewed candidate after fresh scoped verification.
The dispatcher alone owns development Git finalization. Attended public
continuation requires separate authorization and a clean, pushed development
packet; unsupported external state stops without repair.

Closeout corrects stale accounting and seal claims, replaces the deficient live
receipt with entrypoint output, and adds direct metadata, inventory, serializer,
and safe-diagnostic checks. Product qualification is unchanged. The fixed
release-license constraint is retained; generic operator redesign is deferred.
