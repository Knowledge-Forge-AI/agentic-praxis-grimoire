# APG129 v0.10 integrated readiness

## Status and authority

APG129 and exit 00174 were free at entry and are allocated without substitution.
This is an integrated-readiness closeout with a retained historical-evidence
blocker. Dispatcher-owned independent pre-final review is complete. The terminal
producer amends the reviewed candidate; readiness success is not claimed.

Proposal disposition: **amend**. The original task remains the scope authority.
The bound proposal, work-review findings and cumulative phase changes are
evidence, not successor authority. The bounded readiness changes are retained.

## Entry and preserved boundaries

APG128 is manager-accepted. The current backlog has zero `V010_READY_NOW`
implementation items. CSS-QD-001 through CSS-QD-005 and JS-QD-005 remain active
under their recorded triggers. External gates and optional work remain outside
this phase. All 31 provisional leaves retain their individual provisional
disposition; execution alone is not a maturity receipt.

Topology remains 45 canonical/catalog/projection/discoverable leaves, with
14 stable and 31 provisional. Descriptions retain 11,142 bytes under 11,507.
Read-only Git reference and GitHub commit-API observations re-attested the
supplied public v0.9.0 main, tree and annotated-tag identities.

## Plan-review disposition

1. Add explicit fail-closed v0.10 public-surface routing and review every
   non-private path rather than inheriting historical omissions silently.
2. Bound both delimiter-candidate hashing and terminal-newline handling.
3. Resolve browser authority before execution and reuse it on both receipt paths.
4. Validate recorded receipt scenario ID/group pairs against the shared table.
5. Clear Node raw process results before failures and update both exact owners.
6. Add static alias negatives without broadening the permitted callable set.
7. Re-attest the public base through read-only Git and GitHub API evidence.

## Implementation and verification

Browser authority now comes from one versioned declarative family table shared
by JavaScript and Python. Exact ID/group registration, supported family sets,
receipt pairs and source digests are checked. Unknown and ambiguous selections
fail before execution; both receipt paths reuse the preflight authority.
The complete Browser/UI and Browser Runtime matrices pass 72 and 42 runs.

Report extraction uses monotonic incremental hashing and overlapping 64 KiB
search windows. It preserves accepted persisted bytes and file-observation
safety. Generated valid and invalid cases from 256 KiB through 16 MiB pass;
the largest case examines 79,145 candidates and hashes 33,559,886 extraction/
envelope bytes. Six deterministic mid-loop cancellation controls pass. See the
[reporting architecture](../agent-reporting-architecture.md#read-only-persisted-verification)
for the deliberately narrower extraction bound and remaining library-call
latency limits. Canonical renderer hashing is outside the instrumentation.

The Node probe consumes raw streams and returns bounded failure classifications
from its narrow owner. Escaping production exception graphs and actual pytest
local rendering are checked with synthetic failures; real runtime and CommonJS
qualification remain separate. Static alias negatives preserve the existing
named-function boundary. The complete APG128 focused suite passes.

Development source is version 0.10.0. The current publication workflow's exact
version guards, archive epoch, metadata, package assertions and human documentation
are reconciled without triggering publication. Candidate archives use the phase
entry UTC timestamp; public commit/tag timestamp and publisher identity are
separately frozen by the subsequent authorized release-candidate phase.

Go tests, vet and race checks pass across the source packages. Separate local
external-module and XO-consumer imports pass with race checking. Existing
distribution tooling builds and inspects three target binaries, three Python
wheels, a source distribution, the npm launcher and three platform packages;
host wheel/source-install and npm execution checks pass. These are local artifacts.

The full public projection contains 1,268 paths, including report fixtures and
integration sources omitted from earlier version policies. Its explicit v0.10
policy has 45 skills/projections and 467 critical paths. Maintained policy,
symlink, exclusion and Markdown checks pass; the projection's Go tests pass.
All 15 wrapper help routes and shell syntax pass in a disposable projection.
Generic path-marker false positives were corrected without exclusions: Python
sanitation/negative-test values retain identical runtime values, and the XML
DOCTYPE negative uses a reserved invalid HTTPS origin instead of a local URI.
The maintained confidentiality scanner and affected regressions pass.
Historical Python differential comparison is optional when its separate oracle
is absent; maintained public persisted fixtures remain active. No legitimate
product source was excluded to make qualification pass. Four representative
Markdown documents render without horizontal overflow and 34 local rendered
links/fragments resolve. Licensing and contribution files are unchanged.

The corrected work-stage combined gate passes 3,746 unit tests and 708 integration
tests, with two maintained skips. Unit coverage is 10,177/11,866 statements and
3,689/4,598 branches; integration is 10,204/11,866 and 3,691/4,598; union is
10,746/11,866 and 4,002/4,598. Every maintained component and union gate passes.
The exact 80% integration branch minimum is 3,679, leaving a margin of twelve,
above the readiness target of ten. Coverage policy and source classification
are unchanged. The first combined run exposed one stale archive-epoch diagnostic
assertion; its correction and focused regression passed before the complete rerun.

The combined rerun observed no source changes. Afterward, the Node graph test
was strengthened to inspect all non-caller traceback frames and require at
least one production frame, preventing a vacuous audit; all eleven focused
graph cases pass. Runtime implementation remains unchanged. Documentation and
evidence finalization follow the gate. The closeout evidence below supersedes
this work-stage qualification; internal results are not dispatcher checkpoints.

## Work-review disposition

The terminal disposition is **amend**. Empty or ID-less receipt selections now
fail closed. The receipt writer reads table bytes once for both parsing and
hashing, refuses malformed or missing tables, and rejects a supplied authority
that disagrees with the table. Five direct-writer negatives cover these cases.
The three original failure cases were reproduced before the correction.

Two additional tests invoke actual pytest local rendering for malformed Node
probe output and timeout output. Each requires the intended bounded probe
diagnostic and absence of the synthetic stream sentinel. The existing eleven
exception-graph cases remain active. The critical-path declaration is consolidated
without changing its members. These corrections close work-review findings
one, two, three and seven without another substantive review.

The first closeout canonical run exposed temporary-root inheritance in the new
rendering test: nested pytest reused its parent's environment-selected base
directory, invalidating later fixtures. The inner invocation now selects its
own explicit child base directory. The initial canonical result is retained
as failed and cannot serve as terminal qualification.

Finding four is retained as an evidence-custody blocker, not a demonstrated
verifier defect. Finding five remains a governance question about the accepted
predecessor's workaround evidence; this closeout does not revise predecessor
acceptance. For finding six, the tested-source manifest remains evidence while
the dispatcher must bind the terminal committed tree before any successor.

A closeout report regression invocation observed an intermittent fixture failure:
`TestVerifyRejectsReframedMetadataContradictions` reported no reportable change
relative to HEAD. Three isolated repetitions and the subsequent full Go and
race suites passed without source changes. The initial observation is retained;
its cause is not established and no unrelated fixture repair is claimed.

The closeout projection validates policy, paths, links, confidentiality and
shell syntax without creating Git metadata. Wrapper help in that uninitialized
copy cannot resolve a repository; the earlier successful initialized-projection
help receipt is retained against unchanged wrapper bytes.

## Terminal qualification

The corrected closeout canonical run passes 3,753 unit tests and 708 integration
tests, with two maintained skips and no source changes during execution. Unit
coverage is 10,176/11,865 statements and 3,689/4,598 branches; integration is
10,202/11,865 and 3,691/4,598; union is 10,745/11,865 and 4,002/4,598. All
component and union gates pass. Integration retains twelve covered branches
above the exact minimum of 3,679; no threshold or denominator policy changed.

The complete focused JavaScript/Node/debt suite passes 458 cases, browser-focused
tests pass 59, and all 114 browser executions pass against the corrected runner.
Both nested-runner regressions pass after the temporary-root correction. Go
tests, vet and race pass across all packages; local external consumers pass with
race checking. Delimiter scaling, cancellation, skill-library and record-identity
checks pass. The canonical integration suite includes the maintained package
build/install/inspection boundaries. Earlier consumer dogfood and four rendered
document receipts remain explicitly source-bound; the four document digests were
rechecked rather than falsely described as newly rendered.

All executable and test paths in the terminal code binding match the canonical
gate input. Subsequent edits finalize documentation and retained evidence only;
the final projection and whitespace checks cover those edits. Historical debt
and evidence digests remain unchanged. Public base references and their commit/
tree association were re-attested read-only. The historical primary-report
workaround remains incomplete, so the terminal readiness result stays
`V0100_REPORT_VERIFIER_READINESS_BLOCKED`.

## Consumer observations

The committed Theme Forge consumer was exported to disposable storage; existing
uncommitted work in its real checkout was not used or edited. Node 24.19.0 meets
the studio's recorded minimum. Root, Loom, studio and nested-fixture lockfile
installs succeeded. Root build succeeded, but typechecking still reports source
errors after build. Root and initial Loom tests report failures.

The consumer committed a correction during the first observation. A fresh
committed snapshot was exported and rerun; its HEAD and dirty-state readback
remained stable. Studio typechecking and all 315 tests pass. Its maintained Firefox/WebKit smoke
passes all 12 cases. After resolving the nested fixture's missing installation,
the studio build reaches an authenticated Loom candidate archive prerequisite
and stops. A later readback retains the same committed source but observes
changed uncommitted work; the dogfood result remains bound to the committed
snapshot. No archive authority was fabricated and no consumer source was
patched. These observations do not establish APGR causality or actual
Tauri/WebView, native-host, sidecar or assistive-technology qualification.

## Limits, rollback and handoff

The existing JS-QD-005 preservation workaround is not complete: the canonical
APG79B report artifact was unavailable in the inspected archive locations.
Historical evidence files remain unchanged, but they do not substitute for
reading and regenerating the complete primary report. The current disposition
is `V0100_REPORT_VERIFIER_READINESS_BLOCKED` until that artifact is recovered
and the existing workaround passes. The debt itself remains active; this phase
does not implement its trigger-gated feature.

The named-source/named-function JavaScript process proof does not cover arbitrary
reflection, computed calls or newly introduced functions outside its owner set.
Linux cross-build inspection is distinct from Linux runtime qualification.
The report verifier retains the 128 MiB limit and artifact-local proof boundary.
Maturity, debt triggers, dependencies and coverage policy remain unchanged.

The authorized rollback is a coherent reversal of this phase's bounded repairs
and development-version preparation, preserving historical records. Public
v0.9.0, external projects, registry publication and host activation remain
unchanged by this work. No automatic successor dispatch is authorized.

The next release-candidate phase requires accepted readiness and separate
authorization. It must freeze the committed source, public base, publisher and
timestamp, build the deterministic public candidate twice, compare identities,
and prepare the attended publication packet. This work stage does not create
public release objects or publish packages.
