# APG117 v0.9.0 release qualification prerequisites

## Scope and disposition

APG117 begins fresh release qualification of the committed APG116 amendment.
The terminal disposition is `V090_RELEASE_QUALIFICATION_BLOCKED` because the
required release qualification evidence is incomplete. Closeout amends the
producer's explanation after dispatcher-supplied pre-final review. The missing
strict PyPI-compatible renderer limits description rendering only; it is not a
prerequisite for candidate construction, builds, tests, or consumer checks.
No renderer was installed and no generic renderer was substituted.

Proposal disposition: **amend**. The qualification objective remains unchanged.
APG116 is the committed source-amendment branch contemplated by the original
assignment; APG115's superseded descriptions are not selected for qualification.
The producer performed a bounded renderer search. Closeout rejects its use
as a phase-wide required-tool stop. The APG108 incident-specific fallback
removal and regression remain required if executor adaptation proceeds; no
executor was copied or represented as qualified here. No stage deltas were
supplied at entry. Successful live development observation supersedes the
planning-stage network limitation without changing cached tracking.

## Evidence

The source was clean at entry and declares version 0.9.0. Live development
readback matches that committed source; cached tracking is independently stale.
No repush or tracking-ref update was performed. Read-only public refs still
identify the v0.8.1 predecessor and contain no v0.9.0 tag. This observation does
not qualify registries, module contents, checksum history, or public lineage
beyond the observed refs.

Fresh verification checks all 275 retained work-evidence files and 79 retained
closeout-evidence files against their actual sizes and digests. The terminal
inventory covers all 2,714 committed APG115 paths with matching bytes, file
kinds and regular executable status; symlinks use literal target blob bytes.
Historical integrity is established, but no historical test result is reused
as qualification of amended source. The original archive-only incident and its
successful development finalization remain separate, unchanged facts.

The renderer search covers the repository virtual environment, curated Python,
system Python, the active agent tooling's standard virtual-environment location,
uv tools, both standard pipx locations, user Python packages, and standard local
Python executable locations. No strict renderer was found within that scope.
This is a bounded availability claim, not a claim about every file on the host.

## Review disposition and precedent

Closeout accepts review findings F1 through F4. F1 remains unresolved: assignment
sections 4 through 7 were not delivered. The available Git, Go, Node, npm and
Python build toolchain was probed but not used for qualification. No evidence
shows that the renderer prevented those independent operations. This is an
incomplete phase, not a demonstrated environmental block on all qualification.

APG116 already deferred strict PyPI rendering while checking prepared metadata,
sdist metadata, npm archives and links. The v0.8.1 correction likewise recorded
that the exact renderer was unavailable and used a non-equivalent local RDoc
check. Those historical deferrals do not satisfy this assignment's strict
renderer requirement, but they contradict treating its absence as a newly
decisive prerequisite for the entire phase. No maintained renderer dependency
was declared by the inspected build-system configuration.

The historical retained-file count is 354; the containing directory has 356
files because the two packet manifests do not list themselves. Closeout also
records F5's scan limitation: two executable-root rows lacked location labels,
and only names beginning with python or twine were enumerated. Supplemental
location evidence preserves the original observations and does not claim an
exhaustive host search.

## Stop boundary

Closeout retains a blocked result rather than accepting the incomplete release
qualification. It repairs the records under the review's alternative (b);
it does not complete the missing product qualification or waive F1. No public
source defect was demonstrated. Candidate commits and tags, paired ten-asset
bundles, archive and description rendering, prospective module sums, canonical
source gates, installed consumers, examples, publication regression paths, and
operator preflight commands remain unqualified. No older artifact is presented
as a final v0.9.0 asset.

Only affected record identity, local links, privacy, whitespace and evidence
bindings are verified at closeout. No further substantive review was obtained.
The required build and consumer work can proceed independently of the rendering
check in a continuation authorized by the dispatcher. Strict rendering remains
an explicit unqualified item until an existing maintained renderer is supplied
or provisioning is separately authorized. The scope has not been reduced to a
documentation task, and the release qualification objective remains unmet.

Development finalization belongs to the dispatcher. Public publication,
registry mutation, host activation and JACA adoption remain unauthorized. See
[exit 00162](../status/2026/09/08/00162-apg117-v090-release-qualification-prerequisites-exit.md).
