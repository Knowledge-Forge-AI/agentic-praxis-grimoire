# APG67 — JavaScript Language-Profile Architecture and Lean Contract

> APG68 forward disposition: exact APG67 remains immutable proposal evidence.
> APG68 corrected the architecture forward, then rejected ADR 0039 when fresh
> review found material defects after the sole correction. The corrected
> architecture is historical rejected evidence; this record is unchanged
> proposal history.

APG67 begins the JavaScript profile line from exact APG66D with
architecture only: one JavaScript language-profile architecture, one
candidate-independent lean validation contract, one Proposed ADR 0039,
and one frozen publication-excluded scenario register. No skill is
authored, no integration owner changes, and development main remains at
exact APG66D.

## Source verification

The stable annual reference is ECMA-262, 17th edition — ECMAScript 2026,
published June 2026 with the HTML render normative — pinned to the
`es2026` tag of the tc39 source repository, whose tagged source
self-describes as the seventeenth edition. The live repository head has
advanced to the ECMAScript 2027 draft and is classified as moving refresh
evidence with no stable authority. Test262 was verified as bounded
validation and corpus evidence under its actual Ecma BSD-style license —
the expected CC0 orientation did not survive verification and the actual
terms are recorded. Specification-text, source-code, and contribution
rights are recorded separately, and no copied or adapted specification,
algorithm, test, or target expression was identified. Exact object
identities, and one session limitation on independent tree-hash
computation, are recorded in the publication-excluded bundle.

## Target and corpus evidence

Both pinned read-only targets were verified at their exact objects and
never executed. Their complete tracked JavaScript-family inventory is one
handwritten ESM configuration module in the website and none in the
theme; adjacent evidence is TypeScript, Astro, and MDX. Browser scripts,
CommonJS, CLIs, and generated, minified, or vendored JavaScript are
absent from the targets, so no structural claim is target-calibrated. The
APG52 corpus supplies descriptive breadth only, and its family imbalance
independently forbids percentile-derived policy.

## Architecture result

A reusable `javascript-language-profile` coherently owns ECMAScript
language semantics after the effective source-text goal and
implementation context are established. JavaScript, TypeScript, and
Node.js are separate owners; browser/DOM, JSX, Astro/MDX hosts, ECMA-402,
toolchain, security, and project decisions are closed non-owner
boundaries. The effective hierarchy places the actual
parser/runtime/transpiler pipeline first, the annual snapshot as a
bounded reference, Annex B under established context only, owner-supplied
facts as version-bound input, and later drafts without authority.
Script/Module/CommonJS, ESM-syntax-versus-resolution, dynamic-import,
import.meta, and hashbang boundaries are closed. Structural policy is
disposition C — qualitative responsibility-and-complexity-first — with
fifteen named signals, sixteen purpose controls frozen by digest before
selection, no numeric whole-file bands, and no inheritance from the
rejected numeric work.

## Lean contract and register

The contract owns closed owner, selection, response, route, rollback, and
source-boundary vocabularies and a sixteen-field row schema. The frozen
register holds exactly forty rows: thirty-eight candidate-semantic
scenarios and two review-process invariants that no future candidate may
satisfy or explain. Every semantic row has one exact consequence-bearing
result; routing never lowers severity; the register consumes and never
authorizes vocabulary. Machine-checkable claims stay narrower than human
semantic review, and future scripted checks must be mutation-negative,
inheriting the Markdown evidence corrections.

## Eligibility and handoff

Authoring eligibility is `authoring-eligible-with-narrowing`, with eight
exact narrowings recorded in the architecture. ADR 0039 is Proposed only.
The publication-excluded bundle records the baseline parity, source and
rights review, target and corpus inventory, owner and boundary analyses,
structural-policy analysis, scenario register, lean evidence model,
clean-room review, review dispositions, and the Codex APG68 handoff.
APG68 — recommended, not begun — independently reverifies sources,
rights, targets, and corpus, replays the register, applies at most one
coherent correction, and terminally decides ADR 0039.

## State

Development remains 29/29/29 with 14 stable / 15 provisional maturity
and 27/1/28 routes; the Markdown profile remains retained provisional;
CSS remains absent with its rejections terminal; corrected public and
active v0.4.0 are unchanged; the targets are unchanged and unexecuted.
The branch delivery result and report identities are recorded in exit
00100 and the managed report pair.
