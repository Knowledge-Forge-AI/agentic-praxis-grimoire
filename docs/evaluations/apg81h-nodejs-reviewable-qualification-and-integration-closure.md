# APG81H Node.js reviewable qualification and integration closure

Status: State A and candidate-preserving State B are mechanically qualified, current-development release validation passes, and historical identity and prior-qualification binding pass; historical current-runtime replay remains `DIAGNOSTIC_NONQUALIFYING`, and final external review is a commit gate not self-attested by this tracked record.

APG81H is the human-authorized continuation from the truthful APG81G repair checkpoint. It permits one bounded correction of the repo-local task-scratch boundary, replacement-ref-sensitive chronology, exact Node-only topology-transition proof, and the incomplete APG81F source-evidence ledger. It clarifies that independently reviewable integrated State A and rollback State B qualification belongs only after an immutable zero-finding correction review.

The APG81G checkpoint baseline received a fresh zero-finding lifecycle review. Before correction implementation, APG81H reproduced the default-temporary-directory escape class without creating outside task state, froze the complete APG81F omission list, and recorded topology observations. Focused and configured-unit diagnostics passed, but fresh non-author correction review returned C0/H0/M1/L0. The frozen evidence recorded catalog maturity rows rather than the complete actual language-profile lifecycle map and therefore omitted Node's current repair-required lifecycle; the synthetic positive also did not bind every current profile. The attempted implementation and test bytes were discarded before commit, and integration did not open.

ADR 0046 remains Proposed. Node remains retained, repair-required, and unintegrated at 33 canonical candidates, 32 catalog rows, and 32 projections. No Node or scratch debt is accepted. Public and active corrected v0.4.0, targets, exact APG79E main, and the existing CSS and JavaScript debt remain unchanged. No integration, main adoption, stable maturity, readiness, target execution, publication, deployment, APG82, or successor work has begun.

The authorized recovery reimplements the bounded correction and closes the
retained Medium without conflating lifecycle with maturity. Canonical profile
discovery yields 22 current profiles; exact maintained lifecycle and ADR owners
yield one normalized row for each, including Node as
`repair-required|Proposed|unintegrated`. Exact transitions preserve every
non-Node row value-for-value and allow only the declared Node delta. Recovery
failing-first, omission/drift mutations, replacement-ref chronology, M4
evidence completeness, current Node qualification, and configured-unit
evidence pass. Integration remains unopened pending fresh independent
correction review and immutable correction delivery.

The first committed recovery correction at
`7758e801fc7f604e107395c46be741a857d19e5d` is immutable. Its initial parent
C0/H0/M0/L0 review was later superseded for integration qualification by the
final cross-family Claude M1/M2 findings and Sol's acceptance of both findings:
ordinary lifecycle values were maturity-shaped synthesis, and the observed
and former authoritative rows shared one producer. APG81H therefore applies a
forward lifecycle-authority correction without amending that history.

The corrected architecture retains the 22 canonical-profile domain and reads
the five exact current explicit owner pairs for CSS, JavaScript, Markdown,
Node.js, and TypeScript. The remaining seventeen profiles have no explicit
current lifecycle owner and derive `not-applicable`; maturity and integration
remain separate. Node's leaf and specification both state
`repair-required|Proposed`, while catalog and projection membership remain
absent. The candidate-stage skill-library test derives its expected result from
those lifecycle owners and requires the exact intentional 33/32/32 checker
failure. Focused, configured-unit, Node two-runtime, and relevant integration
gates pass; the frozen forward-correction review is C0/H0/M0/L0 with zero Node
and scratch debt. Integration remains unopened at the correction commit.

The next configured qualification exposed one adjacent maintained-test premise
collision: the generic change-size negative assumed pytest `tmp_path` was
outside every Git worktree, while APG81H correctly keeps pytest beneath the
repository-local phase scratch root. The authorized test-only correction uses
a real bare repository below that `tmp_path`, positively verifies its bare and
non-worktree state, and preserves the real exit-2 `git error:` boundary. The
complete change-size integration file and focused change-size, record-identity,
lifecycle, skill-library, and exact two-runtime Node selections pass. Production
change-size code and its closed Git environment remain byte-identical; no Git
ceiling is propagated. Frozen Sol review is C0/H0/M0/L0 with zero Node and
scratch debt. Node remains repair-required and unintegrated and ADR 0046 remains
Proposed while fresh State A qualification resumed.

The current State A integration tree preserves both immutable corrections and
adds only the intended Node integration-owner family. It selects ADR 0046 as
Accepted with amendment, gives Node the independent lifecycle
`provisionally-integrated`, adds the provisional catalog, projection, route,
project-selection, current-release, and maintained-test owners, and produces
33/33/33, 14 stable / 19 provisional, and 31 general / one ChatGPT-local / 32
checked routes. The candidate-preserving State B keeps ADR 0046 Accepted with
amendment and changes Node only to `accepted-integration-rolled-back`,
unintegrated, with no maturity row. Every non-Node topology and lifecycle
member remains exact. Public and active corrected v0.4.0, targets, the ten
CSS/JavaScript known-debt entries, and zero Node and scratch debt remain
unchanged.

Mechanical State A and State B qualification is recorded as completed only
after the exact new review objects and configured gates exist. Final staged
Sol and Claude review is an external commit gate; this tracked tree does not
self-attest its result. No stable maturity, readiness, target execution,
publication, deployment, APG81I, APG82, or successor work is granted.

The superseded diagnostic intended-State-A review object
`8cc0d881ba454772d2254ff5de2750c011f06ffb`, tree
`08137b6443a88507fa6ecc3e21404012c0cab796`, has exact parent
`fa142e1c86fe940ace2d581700bb29e6d7f19c37`. Configured unit passed 3,146
tests at 6,903/7,971 statements and 2,337/2,914 branches. Configured
integration behavior passed 599 tests with two expected skips, but its branch
coverage was 2,268/2,914, below the owned 80% component threshold. The retained
per-file report records 69 missing branches in the already-committed
`libexec/apg_skill_topology.py` owner. State A mechanical qualification
therefore failed, State B was not constructed, final review did not open, and
no integration, report append, rollback proof, or main adoption ran. No debt
is accepted.

The bounded coverage continuation preserves that failed object as diagnostic
history and extends the existing lifecycle/topology integration owner with
real repository-file and production-observer cases. The complete owning file
passes 8 tests and 42 subtests; focused coverage exercises 256/275 statements
and 115/118 branches in `libexec/apg_skill_topology.py`. Fresh configured
integration behavior passes 604 tests with two expected skips at 6,965/7,971
statements and 2,334/2,914 branches, satisfying the unchanged 80% branch gate.
No production lifecycle/topology byte or coverage-policy byte changed. A fresh
State A review object and all remaining configured State A gates are required
before State A qualifies; State B is constructed only after that qualification.
Final staged Sol and Claude review remains external and pending.

The continuation subsequently produced provisional review objects for the
coverage-closed State A and its candidate-preserving rollback State B. The
configured State B combined run passed 604 integration tests with two expected
skips; its unit component covered 6,899/7,967 statements and 2,337/2,914
branches, its integration component covered 6,961/7,967 statements and
2,334/2,914 branches, and the union covered 7,437/7,967 statements and
2,625/2,914 branches. A deterministic local v0.5.0 candidate was then built
from State A over the exact corrected-v0.4 lineage, but candidate-topology
validation failed because the maintained Node source-evidence unit owner
unconditionally reads a publication-excluded `private/evaluations/apg81h/`
file. A focused rerun under the qualified separate bare-worker and bare-pytest
temporary topology reproduced the missing private file directly. This is
outside the coverage-only correction authority. State A therefore remains
unqualified, final external review did not open, and no integration commit,
rollback proof, local-main adoption, or push ran.

The corrected release-test isolation recovery accepted staged tree
`09603ed22c397a09695c7cfe7fb6b24eb35b4757` as the live blocked-release
snapshot because its complete diff fingerprints match the preceding blocked
release. It moved only the APG81H-private M4 source-evidence checks from the
release-selected Node candidate-contract test into a publication-excluded
phase validator. The release-selected test then passed without a private-file
dependency; the phase-only validator passed its positive, omission, and absent-
evidence checks. Fresh configured unit passed 3,126 tests, configured integration passed
604 tests with two expected skips, and unit, integration, and union branch
coverage were respectively 2,337/2,914, 2,334/2,914, and 2,625/2,914 under the
unchanged policy.

Fresh diagnostic release construction confirmed that the repaired Node test
no longer depends on private evidence, but isolated validation exposed a
separate release-environment incompatibility. The validator sets
`GIT_NO_REPLACE_OBJECTS=1`; the unchanged replacement-ref control in the
record-identity integration test expects an ordinary child Git process to
observe the replacement. The exact release-copy test fails with that variable
and passes when only it is removed. This finding is outside the single
authorized M4 ownership repair, so no forward correction, fresh State A or
State B review ref, final external review, integration commit, rollback proof,
local-main adoption, or push ran. No Node or scratch debt is accepted.

The replacement-control continuation repaired only the maintained ordinary Git
positive control. Its child environment now removes only
`GIT_NO_REPLACE_OBJECTS`; a raw child under the hostile outer environment
proves the opposite result, while production chronology independently retains
replacement suppression. The complete record-identity integration owner passes
18 tests under `GIT_NO_REPLACE_OBJECTS=1`, its unit owner passes 15 tests, the
release-portable Node contract passes 32 tests, and M4 private validation remains
release-excluded after its phase-only validation run passed. Fresh configured development qualification passed
3,126 unit tests and 604 integration tests with two expected skips. Unit coverage
was 6,903/7,971 statements and 2,337/2,914 branches; integration coverage was
6,964/7,971 statements and 2,333/2,914 branches. A diagnostic repaired v0.5.0
release, commit `c39a2d7eafe2a2bae217238eaaa2f9018449a3b1`, tree
`1987c2c7070067e12a67ea081f5910e366dd450f`, annotated tag
`ca83bdb88311cefe754af5d28b22170dd57163e1`, passed its isolated built-topology
validation without globally removing the release-owned suppressor.

Exact historical corrected-v0.4 reconstruction retained commit
`d23a09477a2da8e4ea2214910a5a0de6e9be24b5`, tree
`75639775ec7174135a4c6863721cb684f026bd28`, and annotated tag
`96c2647d177c0d03500a8cf7380c56a56d69188e`. Its current-runtime isolated
configured validation passed all 321 unit tests but failed the unchanged 80%
branch gate at 1,542/1,928 branches. Integration passed 268 tests with two
expected skips and failed three historical environment-sensitive controls: one
nested standalone run inherited the same branch shortfall, and two release
controls assume macOS system-temporary symlink aliases or embed their ambient
absolute temporary root. APG81H requires every phase-owned root to remain a
direct descendant of its repository-local phase scratch and does not authorize
historical-code changes or system-temporary fallback.

The maintainer reclassified that current Python 3.13.12 and coverage.py 7.15.2
replay as diagnostic compatibility evidence, not an integration gate. The
historical regression gate instead requires exact immutable object identity,
deterministic historical reconstruction, exact current-only exclusion, and a
binding to already-existing qualification evidence for those exact objects.
The binding is explicit: `private/evaluations/apg43/authority-and-identities.md`
names the corrected commit, tree, and annotated tag above;
`docs/evaluations/apg43-v0-4-notice-brand-correction.md` and
`docs/status/2026/07/26/00063-apg43-v0-4-notice-brand-correction-exit.md`
record two object-equal corrected candidates and passing release,
skill-library, record-identity, Python, Bats, configured, fresh-public-checkout,
and active-source checks; and `private/evaluations/apg43/terminal-review.md`
records the same qualification and deployment boundary. APG81H changes no
historical byte or historical release contract. The diagnostic remains
nonqualifying, while coverage, direct repo-local scratch, and privacy policy
remain unchanged. No debt or exception is accepted.

Final external review then found that five release-owned Node fixture and
contract authority blocks still described the earlier
`authored-proposed-unintegrated` / `APG81A` checkpoint even though intended
State A is `provisionally-integrated` under APG81H. The in-scope correction
updates the fixture README, manifest, scenario authority, two contract owners,
their exact projection hashes, and the current-development release count from
thirty-one to the actual thirty-three. It changes no runtime observation,
candidate semantics, integration owner, historical byte, privacy boundary, or
coverage policy.

The corrected fixture/candidate contracts pass 51 focused cases, the integrated
Node owner passes four focused unit cases, and the exact two-runtime owner passes
13 integration cases. Fresh configured State A passes 3,126 unit tests and 604
integration tests with two expected skips. Unit, integration, and union coverage
are respectively 6,903/7,971 statements and 2,337/2,914 branches,
6,965/7,971 and 2,334/2,914, and 7,441/7,971 and 2,625/2,914. Release
reconstruction and the repeated external review remain precommit gates owned by
the executing recovery and are not self-attested by this tracked record.

The repeated external review found one remaining Medium narrative defect: ADR
0046's opening status still described the superseded failed branch-coverage
object even though the same ADR and every current phase owner record completed
State A and State B mechanical qualification. The in-scope correction makes the
opening status current while preserving the failed object as diagnostic history.
It also corrects State B's fixture README to name APG81H as the current contract
owner and updates the closed README and manifest-projection hashes. No product,
runtime, topology, lifecycle, release-selection, historical, privacy, or policy
behavior changes. The affected 51 State B candidate/fixture contract cases pass;
release reconstruction and repeated final review remain external precommit gates
and are not self-attested here.

The next frozen Sol and Claude review agreed on one remaining Medium
release-surface defect: the APG81H catalog narrative still described the
superseded integration branch-coverage failure. The correction states the
completed State A and State B mechanical result and converts two equivalent ADR
historical sentences to past tense. Review also identified that State B's
retained canonical candidate intentionally makes `apg-project-skills` fail
closed against the rolled-back integrated inventory. A rollback-only real-tree
test now binds that exact consequence rather than silently filtering it away.
This changes no State A production byte or State B production rollback byte;
release reconstruction and fresh final review remain external precommit gates
and are not self-attested here.

Definitive review of that frozen tree returned no material finding and only
historical-tense follow-ups in ADR 0046 and this exit's stopped checkpoint. The
editorial correction closes the full frozen Low set without changing product,
test, topology, lifecycle, rollback, or policy behavior. Release reconstruction
and final confirmation of the corrected tree remain external precommit gates
and are not self-attested here.
