# APG42 v0.4 Release Publication and Active Deployment Exit

Phase ID: `APG42`

## Status

Complete — v0.4.0 published and active public-backed source deployed with provisional limitations

## Scope and result

APG42 publishes the exact APG41-accepted surface as one append-only public
v0.4.0 release commit and one annotated tag, verifies the live public objects
and a fresh public checkout, and advances the existing aggregate-owned active
public-backed source by exact fast-forward.

The release contains 28 canonical skills, 28 catalog rows, and 28 projections,
with 14 stable and 14 provisional rows. It preserves 26 general routes, one
ChatGPT-local direct route, 27 checked edges, and no mandatory chain. ADR 0025
and ADR 0027 remain Rejected; ADR 0026 remains Accepted and controlling.
`matryer-is-test-profile` and `go-testing-stack` remain absent.

The release source retains the exact canonical Agentic Praxis Grimoire NOTICE
blob `d622b081f4cf109501d03bbfebd1224a3ecbcf33`. An unrelated project identity
was not maintainer-approved and is absent from the corrected release and active
source. APG42 changes no skill procedure or maturity row.

## Verification boundary

The associated operational report is required for external acceptance. It must
prove the exact public push, fresh public checkout, active fast-forward,
aggregate preservation, development parity, candidate equality, historical-ref
preservation, tests, reviews, cleanup, and any pending fresh-client smoke.

Current-host verification covers release tooling and lineage, exact projection,
skill and route checks, configured Python and Bats suites, isolated user/project
lifecycle, and active mechanical integration. Source/fixture-reviewed
provisional domains remain grouped in the APG42 evaluation. Universal
cross-platform or target-repository compatibility, automatic invocation,
Docker/Vagrant/Nix/database runtime operation, external services, and production
warranty are not claimed.

## Rollback and preservation

Public history is append-only. APG42 does not force, rewrite, delete, or replace
the v0.4.0 tag. Exact public v0.3.0 remains the identified rollback source, but
no backward active movement is authorized after the fast-forward. The aggregate
integration remains aggregate-owned; no schema-version-1 direct-link migration,
link recreation, or Codex configuration change occurs.

## Deferred and prohibited work

No GitHub Release, signing, announcement, plugin publication, target-repository
mutation, Docker, Vagrant, Nix, database, container, virtual-machine, registry,
cloud, or external-service operation belongs to this phase. No maturity
promotion occurs.

No phase after APG42 is authorized.
