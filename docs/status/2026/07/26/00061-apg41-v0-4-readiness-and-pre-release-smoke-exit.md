# APG41 v0.4 Readiness and Pre-Release Smoke Exit

Phase ID: `APG41`

## Disposition

Complete — v0.4.0 ready for publication with provisional limitations

## Readiness result

Exact APG40 entry, managed-report integrity, ancestry, historical authoring
branches, and read-only external baselines pass. Development remains 28
canonical skills, 28 catalog rows, and 28 projections, with 14 stable and 14
provisional rows. The general router has 26 entries, the ChatGPT-local router
has one entry, and 27 route edges are checked.

All fourteen provisional rows receive explicit trigger, non-trigger,
owner-boundary, project-input, adverse-or-stop, evidence, limitation,
disposition, and removal review. `pytest-test-profile` is
`ready-provisional`; the other thirteen rows are
`ready-provisional-with-limitation`. No maturity promotion occurs.

One coherent wording correction makes Minitest, Dockerfile, and Vagrantfile
removal candidate-independent, removes each public scenario fixture, and
eliminates obsolete embedded catalog counts. Focused contracts and fresh
corrected-state review pass. No retained behavior defect, architecture change,
new skill, or ADR is required.

## Candidate and smoke

Two repository-owned disposable v0.4.0 builds agree on their intended
reproducible fields and contain the exact current public surface. Candidate
checks, current and historical release policy, licensing, provenance, privacy,
modes, relative projections, and local links pass. The candidate contains no
publication-excluded content, development-only path or Git identity,
`matryer-is-test-profile`, or `go-testing-stack`.

Isolated public-surface and skill-library checks, direct router validation,
user installation, project full/repeated/subset projection, invalid and empty
input behavior, removal, and uninstall pass on the current host. Temporary
candidate and smoke roots are removed. Public and active v0.3.0 remain
byte-preserved at 19/19/19.

## Architecture and limits

ADR 0025 remains Rejected, ADR 0026 remains Accepted and controlling, and ADR
0027 remains Rejected. The two retained Go test component owners remain direct
and independent; the stack and matryer/is candidate remain absent.

Static and current-host evidence does not claim cross-platform support,
target-language execution, Docker or Vagrant operation, Nix evaluation or
build, database operation, external-service behavior, publication, active
deployment, signing, or announcement.

## Verification and review

Focused readiness and corrected contracts, complete configured unit,
integration, and combined suites, candidate reproducibility, isolated smoke,
skill and release checkers in text and JSON, record identity, compatibility,
privacy, provenance, rights, Markdown, local links, compilation, configured
Bats execution, command help, strict inventory, and whitespace pass. No current
wrapper or helper is a `/sh` or `/bash` launcher. Fresh non-author reviews
accept all dogfood lanes, the corrected state, compatibility, candidate,
isolated smoke, historical release compatibility, complete diff, and terminal
disposition with no unresolved material finding.

Commit, push parity, and associated managed-report evidence remain required
before external acceptance of this exit.

## Authorization boundary

The readiness disposition authorizes nothing. No phase after APG41 is
authorized. Publication, active deployment, signing, announcement, and any
successor phase require a separate explicit maintainer instruction.
