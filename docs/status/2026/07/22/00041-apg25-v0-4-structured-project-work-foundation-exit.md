# APG25 v0.4 Structured Project Work Foundation Exit

Phase ID: `APG25`

## Disposition

Complete — v0.4 structured-project architecture and roadmap accepted

## Date

2026-07-22

## Accepted result

APG25 accepts ADRs 0020-0022 and five normative documents for structured
formal/non-phase work, testing and coverage, Python-first reporting, ChatGPT
manager topology, and the dependency-ordered v0.4 roadmap.

Formal manager assignments may now reference the repository's structured
defaults and state deviations instead of copying ordinary commit, status/exit,
ADR, docs-only, scoped-test, and report procedure. One bounded correction to
`composing-approved-roadmap-assignments` implements that behavior while
preserving authority, acceptance, stop, and successor boundaries.

The accepted test design selects scoped ordinary tests and a future APG pytest,
xdist, pytest-cov, branch-coverage, mirrored-path, eight-worker architecture
with independent 80/80 and combined-union 85/85 gates. Coverage remediation
will remain an existing-process-owner correction rather than a duplicate skill.

The accepted report design selects a shared Python core with fixed-argument Git
CLI adapters, exact current show semantics, a future temporary-index
`git-diff-report`, and same-report operational composition. The accepted
ChatGPT topology uses nested canonical owners, flat Codex projections, and a
dedicated non-mandatory subrouter.

## Private transition and preservation

The three personal hygiene skills receive bounded future recommendations:
docs-only decommission evaluation, Git-history scope-reduction evaluation, and
RepoMap-phase decommission evaluation. Each requires source-qualified shadow,
private-only behavior disposition, rollback, review, and separate human
authority. None is changed in APG25.

Private development remains nineteen canonical skills, nineteen catalog rows,
nineteen projections, fourteen stable rows, five provisional rows, and eighteen
routable non-router capabilities. Public v0.3.0 and active integration remain
unchanged.

## Validation and review

The phase records a failing-first static contract test and separate
positive/non-trigger scenario review for the single skill correction. Resulting
skill-focused, checker, documentation, identity, privacy, link, Python
compilation, applicable shell syntax, and whitespace gates pass. The complete
APG suite is deliberately not run because this is not a release checkpoint.

Fresh non-author review required and then accepted corrections to phase-default,
testing/reporting, topology, transition, and roadmap evidence. The complete
resulting-tree review has zero unresolved material defects.

## Deferred work and next boundary

APG25 implements no new profile, report conversion, test migration, ChatGPT
move, subrouter, personal-skill removal, readiness gate, or release. Later
roadmap slices remain unallocated.

APG26 is the next and only supplied authorization. It may begin only after this
phase is committed, pushed, remote-equal, and fully reported. APG26 is limited
to evaluating and conditionally implementing `pytest-test-profile` and
`converting-bash-scripts-to-python`; no successor begins automatically.
