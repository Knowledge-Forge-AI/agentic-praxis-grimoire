# APG58 CSS Language-Profile Pilot Authoring Exit

Date: 2026-07-29
Phase ID: `APG58`
Result: Complete — CSS language-profile pilot authored with policy-selected
structural limits; ADR 0035 Proposed and Codex validation pending

## Completed

- Preserved the exact immutable APG57 commit, verified its parent, exit
  `00077`, complete managed reports, and remote parity, and based APG58
  directly on it.
- Stopped the family-wide percentile-derivation cycle and reset structural
  limits to explicit engineering policy: exact 300/600/900 nonblank-line
  levels for ordinary handwritten standalone CSS, frozen before target
  placement and not tuned afterward; the corpus and targets serve
  falsification and legacy evidence only.
- Reverified the CSSWG source pin, W3C document-rights terms, review date
  and refresh conditions, both exact target pins, the twelve-stylesheet
  inventory with live-recomputed nonblank counts (median 199, maximum 995),
  embedded style contexts, and exclusion classes.
- Authored one candidate leaf (`skills/css-language-profile/SKILL.md`) and
  one candidate specification with the exact frontmatter and canonical
  headings, the owner/non-owner contract, separate semantic and growth
  axes, one-count and embedded no-additive-count rules, exclusions, and
  legacy/exception behavior.
- Placed all twelve target stylesheets: nine Green, two Yellow, zero
  Orange, and the 995-line cohesive control legacy Red; embedded Astro
  styles routed semantically with no additive count.
- Froze thirty scenarios (`APG58-CSS-001`–`030`) with all nine established
  fields, covering every exact band edge, legacy behavior, small semantic
  Red, one-count, stricter policy, the bounded human exception, and
  removal.
- Proposed ADR 0035 without accepting it, and created the complete Codex
  APG59 handoff recommending — without authorizing — independent
  validation and decision.
- Submitted the package to fresh non-author Claude review lanes; the
  disposition is recorded privately. Claude review is not independent Codex
  validation.

## Verification

Exact APG57 object/report/parity baseline; live target-object inventory and
count recomputation; policy, owner-boundary, semantic/growth-separation,
one-count, legacy/exception, scenario, leaf/spec/ADR, and handoff reviews;
skill-library surface unchanged; record identity, Markdown/link, privacy,
clean-room, rights, change-size, whitespace, concrete-message, and
complete-staged-diff checks. No APG unit/integration/combined/Bats suite ran
(no maintained executable ownership changed); no target install, build,
test, lint, preview, browser, or deployment command ran.

## Preservation

ADR 0031 and ADR 0034 remain Rejected; all ten former Web/Node candidates
remain deferred; no other profile was authored. Integrated development
remains 28/28/28 with fourteen stable and fourteen provisional; the
candidate leaf is unintegrated and adds no catalog row, projection, route,
maturity row, release row, executable test, or dependency. Corrected public
and active v0.4.0 are unchanged. No prior history was rewritten; no target
repository was mutated or executed.

## Next authorization

None. APG59 — independent Codex verification of the exact APG58 object,
failing-first executable contracts, the policy challenge, at most one
coherent forward correction, the ADR 0035 decision, and
integration-only-if-retained — is recommended in the private handoff and
requires separate explicit maintainer authority, as does every other
successor, readiness, publication, or deployment step.
