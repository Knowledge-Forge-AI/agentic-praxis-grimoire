# APG52 Reproducible Web and Node Evidence Foundation Exit

Date: 2026-07-28
Phase ID: `APG52`
Result: Complete

## Completed

- Reproduced the APG51 corpus package as not fully reproducible using only
  committed evidence.
- Recovered every abbreviated APG51 source-family identity to a full commit
  and tree without silent substitution.
- Added tested, standard-library-only, publication-excluded exact-Git-object
  corpus tooling and closed manifests.
- Recorded complete artifact membership, exclusions, measurements, exact
  statistics, per-source-family distributions, deterministic review sampling,
  hypothesis comparison, and an exact rights ledger.
- Reverified the canonical JSX README CC BY 4.0 control and preserved the
  target TypeScript consumer result.
- Produced byte-identical canonical outputs from two disjoint acquisition and
  output roots.
- Preserved both targets, corrected public and active v0.4.0, and the complete
  APG50/APG51 history.

## Evidence disposition

Nine whole-file classes are `reproducible-sufficient`. Scoped CSS is
`insufficient-source-family-count` because no section artifacts were selected.
Standalone JSX/TSX is `insufficient-source-family-count` with seventeen
artifacts from two selected families. Neither limitation was padded.

APG51 bands remain non-normative hypotheses. Nine comparisons are
`original-rule-not-recoverable`; scoped CSS and standalone JSX/TSX are
`insufficient-evidence`. No comparison claims consistency merely from corpus
sufficiency.

## Architecture disposition

ADR 0031 remains Rejected. ADR 0032 was not created. All ten Web and Node
candidates remain deferred, React and Vitest retain Policy A, and no authoring
slice is eligible. Development remains 28/28/28 with fourteen stable and
fourteen provisional skills. No skill, catalog, projection, route, maturity,
dependency, release-policy, public executable, target, public release, or
active installation was changed.

## Verification boundary

Focused private unit and real-Git integration tests, Python compilation,
two-run byte comparison, exact JSON/JSONL parsing, rights and privacy review,
skill-library text/JSON checks, identity checks, Markdown/link checks, and Git
diff checks pass. Complete, readiness, smoke, release, publication, and
deployment suites were not run because they are outside APG52.

## Next authorization

None. A successor architecture decision, profile authoring, additional corpus
phase, adjacent browser/HTML/accessibility work, target mutation, or release
action requires separate explicit maintainer authority.

## Subsequent storage note

APG53 later removed the three complete regenerable JSONL datasets from the
current tree after exact hash, count, and regeneration verification. Compact
accepted evidence and external-output regeneration remain. APG52 history and
this outcome are unchanged.
