# APG116 v0.9.0 release wording amendment exit

Phase ID: `APG116`

## Status and scope

Terminal source-amendment disposition: `V090_SOURCE_AMENDMENT_REQUIRES_QUALIFICATION`.
APG116 records the bounded amendment found during release qualification of APG115.
Dispatcher pre-final findings were dispositioned at closeout; development
finalization remains dispatcher-owned.
No public publication or successor is authorized by this record.

## Proposal and review disposition

Proposal disposition: **amend**. All four advisory planning findings are accepted:
expand the first gate to every identified package-facing surface; explicitly
deliver an amendment record and non-executable packet skeleton; defer complete
author/timestamp-bound operator commands until the executor is qualified; inspect
the stale epoch alias without changing historical archive policy. No stage deltas
were supplied before work. Closeout accepted the supplied work deltas with the
bounded corrections below. Successful live development observation supersedes the planner's
network limitation; stale cached tracking is separately disclosed.

## Demonstrated defect and correction

Actual retained prepared metadata, wheel metadata and sdist metadata contain
instructions telling v0.9.0 production users to remain on v0.8.1. Permanent
release descriptions must remain useful after publication without prematurely
asserting it occurred. The amendment uses version-scoped and conditional
installation language across the affected descriptions and documentation.
Historical phase records and released consumer requirements are preserved.

## Verification

Historical packet integrity checks passed for 275 work-evidence files and 79
closeout-evidence files. All 2,714 terminal source-inventory entries match the
committed APG115 blob bytes, kinds and regular executable status. The supplied
manifest and inventory digests were verified from actual files. A first verifier
attempt incorrectly compared complete local permission bits with Git modes; the
corrected comparison preserves Git's executable-bit semantics and changed no
historical evidence.

Fresh parent verification passed **139 tests and 152 subtests** across the Python
publication, npm distribution and public-release unit owners plus the real-Git
public-release integration owner. Generic future-version npm placeholder coverage
remains active. Actual prepared Python metadata and sdist PKG-INFO match the full
amended README description, version, content type, summary, license and project
URLs. All four npm fixture tarballs carry the rendered README and discovery
metadata; their synthetic binaries make this metadata evidence, not native
execution or final release-asset evidence.

The repository policy gate, record identity and whitespace checks passed under
the pinned repository Python environment. Bounded local/future-version Markdown
link checks passed against the amended worktree. Future publication URLs were
not represented as successful live requests. Strict PyPI rendering is deferred:
the repository Python environment has no installed strict renderer.

One bounded Gemini worker completed with cleanup proven. Its changes were
accepted with parent amendments preserving the released control lane and narrowing
a prose assertion. Its host-environment policy attempt reported an incorrect
coverage version; the parent's pinned-environment policy check passed. Worker
tests used vendor-managed temporary/log storage; parent checks used owned shared
scratch. No vendor cache was copied or removed.

The first parent metadata verifier interpreted UTF-8 Markdown as implicit ASCII
and failed full-description equality. Explicit UTF-8 parsing corrected that
verifier; the failed receipt remains preserved. The historical full gate is not
claimed as qualification of the amended source. Result-only record updates are
separately checked and bound after affected commands. Closeout verification is
recorded separately from these work-stage receipts.

## Deferred work and boundary

No final public candidate, ten-asset pair, new module sums, registry/history gate,
or tested publication executor is delivered in this amendment branch. Qualification
must start from the amended committed source; older artifacts are not final assets.
The packet skeleton does not offer executable preflight or publication commands.

The original archive-only incident and successful development finalization remain
separate historical facts. No old run, cache or release packet was altered.
The dispatcher alone owns development staging, commit and push. Public mutation,
npm authentication, host activation and JACA adoption were not performed.

## Closeout disposition

Disposition: **amend**. Independent review reproduced a remaining preparation-only
statement in the documentation index. Closeout corrected it and added a regression
over all eleven audited surfaces; that regression first failed on the index.
The CLI reference attribution and npm sentence were corrected. The external
consumer instructions now distinguish the historical candidate procedure from the
released control. Exact prospective-lane commands remain deferred to qualification
of the amended committed source.

Closeout affected Python-publication and npm-distribution tests passed (78 tests).
The final bounded metadata/link and record checks are retained in the amendment
packet. This evidence does not qualify a ten-asset release bundle or installed
consumers. No additional substantive review was requested.
