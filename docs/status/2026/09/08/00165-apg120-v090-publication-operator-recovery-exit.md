# APG120 v0.9.0 Publication Operator Recovery Exit

Phase ID: `APG120`

## Terminal closeout disposition

Disposition: `V090_PUBLICATION_RECOVERY_PACKET_QUALIFIED`.
Dispatcher pre-final findings are dispositioned; scoped terminal verification passed.

APG120 reproduces the production PyPI missing-key defect and repairs the required
metadata contract and current packet bindings. The bounded recovery entrypoint
accepts only exact published-release successors, preserves existing public
objects, retains npm terminal authentication, and emits per-invocation controlled
stop and unexpected-exception results. See the
[APG120 evaluation](../../../../evaluations/apg120-v090-publication-operator-recovery.md).

Fresh focused tests (61 recovery and 24 selected production cases) and read-only
live reconciliation support the terminal packet.
Git/tag, GitHub Release/workflow, Go, and PyPI are exact; npm remains absent at the
observed `exact_published_release_pypi_prefix`. Product source, candidate, and ten
artifacts are unchanged; APG119 product qualification is reused explicitly.

The proposal is amended with all advisory findings dispositioned. Historical
attempt records are preserved; the first overwritten machine result is disclosed
as unavailable. No public execute command, product rebuild, installation,
publication, host activation, staging, commit, or push occurs during producer and closeout work.
Development finalization belongs to the dispatcher. Public continuation is
separately attended and authorized. No successor is dispatched.

Closeout corrects stale accounting and seal claims, replaces the deficient live
receipt with entrypoint output, and adds direct metadata, inventory, serializer,
and safe-diagnostic checks. Product qualification is unchanged. The fixed
release-license constraint is retained; generic operator redesign is deferred.
