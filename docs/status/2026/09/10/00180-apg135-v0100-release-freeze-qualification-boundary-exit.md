# APG135 v0.10.0 release freeze qualification boundary exit

Phase ID: `APG135`
Exit record: `00180`
Status: terminal closeout blocked; release bundle unqualified.

Proposal disposition: **amend**. The
[evaluation](../../../../evaluations/apg135-v0100-release-freeze-qualification-boundary.md)
records all six advisory amendments, corrected-source preflight and deterministic
candidate A/B construction. APG134's wording correction passes; APG133 remains
historically blocked and supplies no reused artifact.

The governing blocker is the workflow fixture defect reproduced against frozen
APG134 source, requiring corrected source and complete reconstruction. Canonical
directory collection omits the workflow integration file and its unit sibling
beneath `.github`; inventory completeness does not prove pytest collection.

The v0.10 public-check selection does not activate the maintained seven-node
private-input exclusion list because it compares against the older v0.7 file
tuple. A focused projected-candidate run reproduces the excluded-oracle failure.
The canonical coverage route also selects that test. Dependent distribution and
module construction is stopped without patching frozen inputs or weakening
qualification. Canonical unit coverage passes; integration finishes with seven
failures, 701 passes and two skips. The seven failures exactly match the
maintained public exclusions; no union gate passes. Both remaining candidate
checks are deliberately interrupted after this stop condition, with exact
source/base/candidate fingerprint preservation. Their partial results also
reproduce a stale v0.9.0 workflow fixture against the v0.10.0 verifier, confirmed
by a focused source run. No completed candidate-check success is claimed.

Entry and exit public readback preserve v0.9.0 main and its annotated tag, with
no public v0.10.0 tag and HTTP 404 for its public release endpoint. Raw candidate
repositories and test artifacts remain outside the repository in retained task
scratch. Compact manifests, receipts and bounded failure evidence remain private.
The producer reports process and worker cleanup, but independent verification
is unavailable: the receipt contains only scalar assertions. One isolated-
validation temporary root remains recorded and is not claimed removed.

`V0100_RELEASE_BUNDLE_QUALIFIED_FOR_OPERATOR_PREFLIGHT` is not achieved.
There is no qualified ten-asset bundle or publication operator packet.
Terminal disposition: **amend**, retaining the blocked result with reporting-only
corrections. Candidate checks remain incomplete and defect inventory is unknown.
The dispatcher owns Git finalization; qualification-boundary
resolution and any corrected-source reconstruction require separate disposition.
No public publication, authentication, host mutation or automatic successor
occurs.
