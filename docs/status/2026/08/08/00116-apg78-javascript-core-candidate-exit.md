# APG78 JavaScript Core Candidate Exit

Phase ID: `APG78`

## Status

Complete — narrow JavaScript core language-profile candidate and target-first
fixture authored under ADR 0042; ADR 0045 Proposed, candidate unintegrated, and
APG79 independent hardening pending.

## Result

- Authored one narrow reusable `javascript-language-profile` candidate owning
  ECMAScript semantics for an established source region, with its specification
  and a navigation-only twenty-four-scenario coverage record.
- Authored one fourteen-case APG-owned target-first fixture over seventeen
  source files, with every manifest path present and no unowned file.
- Proposed ADR 0045 and recorded the Codex APG79 hardening handoff. ADR 0045 is
  not decided by APG78.
- Kept Node, browser and Web platform, module resolution and loading, build and
  transformation, TypeScript, JSX, React, MDX, Astro, security, architecture,
  and deployment as explicit non-owners with named receiving owners.
- Deferred JavaScript structural policy, added no numeric threshold, and
  authorized no automatic migration in either direction.
- Preserved ADRs 0039 and 0040 as Rejected and used APG67-APG70 only as
  falsification evidence; revived no rejected architecture or candidate prose.
- Ran a bounded scratch-only smoke over a copy of the fixture under one exact
  engine, recording its exact proof boundary. No target command ran.

## Preserved state

- JavaScript current integration: absent. No catalog row, projection, maturity
  row, capability route, project selection, release owner, or maintained test
  owner was added.
- APG78 branch: 32 canonical leaves / 31 catalog rows / 31 projections. That
  transitional shape is expected and truthful; the skill-library checker was not
  weakened.
- Integrated `main`: exact APG77D at 31/31/31.
- Maturity: 14 stable / 17 provisional. Routes: 29 general / 1 ChatGPT-local /
  30 checked.
- CSS: retained provisional with `CSS-QD-001` through `CSS-QD-005` exact and
  unchanged. TypeScript and Markdown: retained provisional.
- Public and active corrected v0.4.0 unchanged. Both read-only targets unchanged
  and unexecuted.

## Delivery limitation

The APG78 candidate was authored but **not committed or delivered by the phase
agent**. The session's Git broker permits read-only Git only: `fetch`,
`checkout`, `switch`, and `branch` are refused, and `add` fails with a broker
filesystem error. The branch `claude/apg78-javascript-core-candidate` was
therefore not created, no commit exists, no push occurred, and no Git-show or
operational report was generated.

No remote parity, commit identity, or report identity is claimed. `main` was not
moved and remains exact APG77D. The exact operator sequence to create the
branch, commit, deliver, and report is recorded in the APG78 private evaluation
bundle.

## Next boundary

Recommend, without beginning, APG79 — Codex JavaScript Core Iterative Hardening
and Provisional Integration, expected exit 00117 and the terminal ADR 0045
decision. This exit grants no APG79 authority.
