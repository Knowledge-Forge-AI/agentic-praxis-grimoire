# APG101 Human Documentation and README Restructuring Exit

Phase ID: `APG101`

## Status

**Complete candidate — the v0.7 human documentation is ready for
dispatcher-owned pre-final inspection.**

Candidate terminal disposition:
`V07_HUMAN_DOCUMENTATION_READY_FOR_APG102`.

Dispatcher-owned staging, commit, push, closeout verification, and final
acceptance remain outside provider work.

## Outcome

The root README is now a concise landing page in the frozen ten-part order. A
new reader can identify APG's provider-neutral toolkit boundary, concrete use
cases, safe source-checkout commands, Go/CLI/Python/npm/Nix consumption
choices, core concepts, JACA boundary, deeper documentation, release status,
and contribution/licensing terms without traversing a phase ledger.

`docs/README.md` provides task-oriented navigation to the frozen detailed
CLI, Go library, skill-context, environment, hotspot, distribution,
integration, governance, release, status, and history owners. The former root
README chronology remains public under
`docs/history/releases-and-phases.md` with corrected relative links and a
clear archaeology boundary.

The hidden `APG-CANDIDATE-STATE` marker remains the root README's mechanical
candidate-state authority and occurs there exactly once. Its machine role is
recorded here instead of explained in the onboarding opening.

## Release truth

Published v0.6.0 remains the latest public release. Development remains a
locally qualified v0.7.0 release candidate; no v0.7 GitHub, PyPI, npm, or Go
module publication is claimed.

APG100 is terminally accepted as the runtime and local distribution baseline.
Its dispatcher result-fence recognition failure changed no accepted source
bytes. Exact development identities remain outside public documentation.

## Preserved state

Version remains `0.7.0`. The corpus remains 39/39/39/39, 14 stable / 25
provisional, zero malformed, 9,504 description bytes, 9,492 description
characters, and the 9,527-byte ceiling with the unchanged canonical
fingerprint.

APG101 changes no runtime API, CLI syntax, schema, packaging behavior,
distribution target, skill, maturity, known debt, JACA, Nix, active
integration, deployment, or publication state.

## Readiness handoff

Every APG101-changed public Markdown document passes the repository's
link-resolution rules against the prospective public path set. The full
prospective link gate separately reproduces an unchanged baseline defect:
`hotspot/testdata/classification/sample.md` links to an absent
fixture-relative `target`. APG101 does not alter that test fixture or the
public-projection filter; APG102 must disposition it before release.

## Next authorization

APG102 may begin only under a separate dispatcher assignment. It owns
cross-consumer release readiness, including real JACA imports and
CLI/Python/npm parity; none of that work begins here.
