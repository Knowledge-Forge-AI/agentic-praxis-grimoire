# APG100 Complete Go Strangler and Multi-Ecosystem Distribution Exit

Phase ID: `APG100`

## Status

**Terminal — the operator accepted the reviewed and corrected APG100 closeout
source as the v0.7 portable runtime and local multi-ecosystem distribution
baseline.**

Terminal disposition:
`V07_MULTI_ECOSYSTEM_DISTRIBUTION_READY_FOR_APG101`.

The preceding dispatcher stopped on result-fence recognition after closeout;
that operational failure changed no source bytes or product disposition. Exact
development identities remain outside this public record.

## Outcome

APG100 changes the single editable version authority to `0.7.0`, completes Go
ownership of response capture, and removes independent Python mutation from
the normal response path. The Python bridge verifies bundled binaries and
retains exact source-checkout development behavior without a runtime download
or unrelated `PATH` fallback.

One canonical target binary manifest binds every supported Go binary to its
version, target, size, SHA-256, corpus fingerprint, build-info schema, build
flags, and build identity. Deterministic local orchestration produces three
platform-specific Python wheels, one Go-source sdist, one npm launcher package,
three npm platform packages, and one coordinated distribution manifest. Each
ecosystem reuses the same target binary bytes.

## Preserved ownership and state

- Go owns portable report, skill, environment, hotspot, and response behavior.
- Python owns repository and host maintenance, including the legacy project
  symlink projection adapter.
- `check skill-library` combines repository governance with a required Go
  embedded-corpus check.
- The v0.7 public candidate excludes duplicate report and skill-consumer Python
  implementations; historical v0.6 reconstruction remains frozen.
- Skills remain 39/39/39/39, 14 stable / 25 provisional, zero malformed,
  9,504 description bytes, 9,492 description characters, and the 9,527-byte
  ceiling with the unchanged corpus fingerprint.
- Report, skill-bundle, environment, and hotspot schemas remain unchanged.

## Qualification boundary

The supported target matrix is `darwin/arm64`, `linux/amd64`, and
`linux/arm64`. Host Python and npm packages are qualified from local artifacts
in disposable roots without a Go runtime dependency. Foreign packages receive
deterministic build and structural qualification; APG102 owns foreign runtime
execution and cross-consumer readiness.

APG100 does not publish, tag, deploy, mutate `.flakes` or Nix, integrate JACA,
rewrite the root README, or begin APG101.

## Next authorization

APG101 subsequently began under a separate dispatcher assignment. It owns the
human-oriented README and documentation restructuring and may not change the
runtime or packaging semantics established here.
