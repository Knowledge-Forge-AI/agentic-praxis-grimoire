# APG99 Structural Hotspot Analyzer Exit

Phase ID: `APG99`

## Status

**Complete — structural hotspot analysis accepted and preserved by APG100.**

Terminal disposition:
`V07_STRUCTURAL_HOTSPOTS_READY_FOR_APG100`.

## Outcome

APG99 adds the provider-neutral public Go `hotspot` package with mandatory
bounded root-safe direct reads, the complete frozen classification/capability
matrix, exact Go AST metrics, honest structural and unavailable metrics,
canonical report JSON and SHA-256 identity, report-local deterministic
ranking, concise terminal output, and detailed deterministic Markdown.

The CLI adds `apgr analyze hotspots` through the established global repository
root. Python delegates exact argv to Go with no semantic fallback. A disposable
external module imports and consumes the public API in process. Growth/churn
is explicitly deferred and the implementation neither shells out nor inspects
Git history.

## Qualification

- Every frozen classification surface has a committed fixture and capability
  declaration.
- Go owner statement, cyclomatic, nesting, parameter, function-literal, and
  generated-file policies have exact test vectors.
- Ranking proves tie handling, capability separation, unavailable-weight
  omission, frozen weights, and deterministic final ordering.
- Root, file, total-byte, time, directory/structured-depth, symlink,
  cancellation, unreadable-file, and drift paths are bounded and fail closed.
- Terminal, complete JSON, detailed Markdown, top-N, filters, limits, and safe
  exclusive output are qualified.
- Python performs exact-argv Go delegation; an external Go module uses the API
  without importing APG internals.
- Immutable APG and read-only JACA source snapshots each repeat with identical
  JSON/fingerprints and no target mutation or execution.

## Preserved state

- Version remains 0.6.0.
- Skills remain 39/39/39/39 and 14 stable / 25 provisional.
- Discovery remains zero malformed, 9,504 description bytes, 9,492 characters,
  and the unchanged 9,527-byte ceiling.
- APG96 report, APG97 skill-context, and APG98 environment behavior remain
  intact.
- No JACA, `.flakes`, Nix, skill, README IA, response, packaging, release,
  deployment, or successor state changes.

## Next authorization

APG100 completed under its separate dispatcher assignment. This exit grants no
README, publication, deployment, or later-successor authority.
