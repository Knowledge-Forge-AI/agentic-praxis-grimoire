# APG89 v0.6 Dogfood, Composition, Context, and Readiness Exit

Phase ID: `APG89`

## Status

APG89 has completed target dogfood, composition, explicit-selection, installed
context, reproducibility, and the bounded coverage investigation. The
implementation/testing candidate satisfies the readiness gate. The later
external supervisory review accepted the exact candidate and terminalized the
current APG89 state as `READY_FOR_APG90`.

The review bound private-development commit
`6c3f21be262265944b12cb1f2781591773916660` and tree
`4520877d455b9040fee222769d1636def2bc7ad0`, with C0/H0/M0/L0 technical
findings. The historical APG89 commit is unchanged. Its private-development
push is distinct from public v0.6 GitHub and PyPI publication.

## Result

1. Three read-only web targets ground Markdown, MDX, Astro, React, JSX/TSX,
   TypeScript, JavaScript, Node, and Vitest; a clean Go target grounds Go,
   native Go testing, GoMock v0.6.0, and go-cmp v0.7.0 composition.
2. Fifteen fixture rows select one narrowest owner each without an aggregate
   owner, mandatory chain, implicit invocation, contradiction, or seventh
   v0.6 profile.
3. Exact 2-, 33-, 9-, and 4-skill project subsets install, check, and uninstall
   without expansion or unrelated-file mutation.
4. Two disjoint package builds are byte-identical, and installed context
   readback matches checkout context exactly.
5. Reproduced branch variance has deterministic coverage owners without any
   production, threshold, exclusion, rounding, or policy change. The residual
   two branches are the live-owned report-lock predicate and positive-PID path;
   their focused owner and five-run fixed-sample execution close the bounded
   investigation.

## Resulting state

- topology: 39 canonical / 39 catalog / 39 projections;
- discovery: 39 measured / 39 discoverable / zero malformed;
- maturity: 14 stable / 25 provisional;
- total description bytes: 9,504;
- total description characters: 9,492;
- remaining headroom under 9,527: 23 bytes;
- project selection: explicit only; and
- package/public/active version: unchanged `0.5.0`.

## Verification

Exact target revisions, inventories, commands, package hashes, owner matrices,
selection cases, installed context readback, and coverage observations are in
the APG89 evaluation. Five standalone integration observations and five
combined observations are green with an invariant 2,865 integration-branch
numerator and 3,177 combined-union branch numerator. The external supervisory
review returned **ACCEPT**. APG90 release preparation, public finalization, and
their own lifecycle evidence remain later work.

## Limitations and stop

Terminal Nova's checked-in pnpm lockfile omits seven declared manifest
dependencies, so its locked install truthfully stops before build. APG did not
patch the target or run an unlocked install. No target or active projection was
mutated.

No version advance, release candidate naming, public publication, upload,
deployment, or APG90 implementation ran in APG89. The terminal disposition is
`READY_FOR_APG90`.
