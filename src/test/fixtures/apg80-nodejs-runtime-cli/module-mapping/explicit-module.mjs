// APG80-FX-002 — `.mjs` maps to Node's ESM loader regardless of package scope.
// The extension is Node host evidence, not an ECMAScript grammar rule.

export const extensionEvidence = 'explicit-mjs';
