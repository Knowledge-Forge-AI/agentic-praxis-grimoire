// APG80-FX-006 — selected only when the runtime is invoked with the matching
// custom condition. The condition set is part of the invocation evidence.

export const selectedCondition = 'apg80-custom';
